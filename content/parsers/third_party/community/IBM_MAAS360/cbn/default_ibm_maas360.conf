# Product: IBM_MAAS360
# Category: Security
# Supported Format: JSON
# Reference: N/A
# Last Updated: 2024-11-06
# Copyright 2024 Chronicle LLC

filter {
  mutate {
    replace => {
      "maas360DeviceID" => ""
      "deviceName" => ""
      "customAssetNumber"=> ""
      "ownership" => ""
      "deviceOwner" => ""
      "username" => ""
      "emailAddress" => ""
      "platformName" => ""
      "sourceID" => ""
      "deviceType"=> ""
      "manufacturer"=> ""
      "model" => ""
      "osName" => ""
      "osServicePack" => ""
      "imeiEsn" => ""
      "lastReportedInEpochms" => ""
      "installedDateInEpochms" => ""
      "mailboxLastReportedInEpochms" => ""
      "installedDate" => ""
      "lastReported" => ""
      "deviceStatus" => ""
      "maas360ManagedStatus" => ""
      "udid" => ""
      "wifiMacAddress" => ""
      "mailboxDeviceId" => ""
      "mailboxLastReported" => ""
      "mailboxManaged" => ""
      "isSupervisedDevice" => ""
      "testDevice" => ""
      "itunesStoreAccountEnabled" => ""
      "unifiedTravelerDeviceId" => ""
      "phoneNumber" => ""
      "osVersion" => ""
      "enrollmentMode" => ""
      "platformSerialNumber" => ""
      "modelId" => ""
      "mdmPolicy" => ""
      "userDomain" => ""
      "firstRegisteredInEpochms" => ""
      "lastRegisteredInEpochms" => ""
      "lastMdmRegisteredInEpochms" => ""
      "policyComplianceState" => ""
      "appComplianceState" => ""
      "ruleComplianceState" => ""
      "selectiveWipeStatus" => ""
      "jailbreakStatus" => ""
      "encryptionStatus" => ""
      "passcodeCompliance" => ""
      "mdmMailboxDeviceId" => ""
      "AppName" => ""
      "appName" => ""
      "appID" => ""
      "appVersion" => ""
      "appSoftwareSize" => ""
      "appDataSize" => ""
      "appManagedStatus" => ""
      "logType" => ""
      "lastSoftwareDataRefreshDate" => ""
      "_relation" => ""
      "maas360Environment" => ""
      "installLocation" => ""
      "principal" => ""
      "target" => ""
      "network" => ""
      "metadata" => ""
      "additional" => ""
      "security_result" => ""
      "has_principal" => "false"
      "has_target" => "false"
    }
  }

  json {
    source => "message"
    array_function => "split_columns"
    on_error => "not_json"
  }
  if [not_json] {
    drop {
      tag => "TAG_MALFORMED_ENCODING"
    }
  }
  if [lastReported] != "" {
    date {
      match => ["lastReported", "yyyy-MM-ddTHH:mm:ss"]
      target => "_entity.asset.last_discover_time"
      on_error => "_check_lastReported_date_format"
    }
  }

  if [installedDate] != "" {
    date {
      match => ["installedDate", "yyyy-MM-ddTHH:mm:ss"]
      target => "_entity.asset.first_discover_time"
      on_error => "_check_installedDate_date_format"
    }
  }
  if [maas360DeviceID] != "" {
    mutate {
      replace => {
        "event1.idm.entity.metadata.product_entity_id" => "%{maas360DeviceID}"
        "_entity.asset.hostname" => "%{maas360DeviceID}"
      }
      on_error => "_maas360DeviceID_empty"
    }
  }
  if [logType] != "DeviceDetails" {
    if [userDomain] != "" {
      mutate {
        replace => {
          "_entity.asset.network_domain" => "%{userDomain}"
        }
        on_error => "_userDomain_empty"
      }
    }

    if [deviceStatus] != "" {
      if [deviceStatus] == "Active" {
        mutate {
          replace => {
            "_entity.asset.deployment_status" => "ACTIVE"
          }
          on_error => "_deviceStatus_empty"
        }
      }
    }

    if [deviceName] != "" {
      mutate {
        replace => {
          "_entity.asset.asset_id" => "%{deviceName}"
        }
        on_error => "_deviceName_empty"
      }
    }

    if [deviceType] == "Smartphone" {
      mutate {
        replace => {
          "_entity.asset.type" => "MOBILE"
        }
        on_error => "_deviceType_empty"
      }
    }

    if [osName] != "" {
      mutate {
        replace => {
          "_entity.asset.platform_software.platform_version" => "%{osName}"
        }
        on_error => "_osName_empty"
      }
    }

    if [osVersion] != "" {
      mutate {
        replace => {
          "_entity.asset.platform_software.platform_patch_level" => "%{osVersion}"
        }
        on_error => "_osVersion_empty"
      }
    }

    if [manufacturer] != "" {
      mutate {
        replace => {
          "_hardware.manufacturer" => "%{manufacturer}"
        }
        on_error => "_manufacturer_empty"
      }
    }
    if [model] != "" {
      mutate {
        replace => {
          "_hardware.model" => "%{model}"
        }
        on_error => "_model_empty"
      }
    }

    if [platformSerialNumber] != "" {
      mutate {
        replace => {
          "_hardware.serial_number" => "%{platformSerialNumber}"
        }
        on_error => "_platformSerialNumber_empty"
      }
    }
    mutate {
      merge => {
        "_entity.asset.hardware" => "_hardware"
      }
      on_error => "_hardware_empty"
    }

    if [wifiMacAddress] != "" {
      mutate {
        merge => {
          "_entity.asset.mac" => "wifiMacAddress"
        }
        on_error => "_wifiMacAddress_empty"
      }
    }


    if [ownership] != "" {
      mutate {
        replace => {
          "ownership_label.key" => "ownership"
          "ownership_label.value" => "%{ownership}"
        }
        on_error => "_ownership_not_mapped"
      }
      if ![_ownership_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "ownership_label"
          }
          on_error => "_ownership_empty"
        }
      }
    }

    if [deviceOwner] != "" {
      mutate {
        replace => {
          "deviceOwner_label.key" => "deviceOwner"
          "deviceOwner_label.value" => "%{deviceOwner}"
        }
        on_error => "deviceOwner_not_mapped"
      }
      if ![deviceOwner_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "deviceOwner_label"
          }
          on_error => "_deviceOwner_empty"
        }
      }
    }

    if [imeiEsn] != "" {
      mutate {
          convert => {
              "imeiEsn" => "string"
          }
          on_error => "_already_string_value_imeiEsn"
      }
      mutate {
        replace => {
          "imeiEsn_label.key" => "ImeiEsn"
          "imeiEsn_label.value" => "%{imeiEsn}"
        }
        on_error => "_imeiEsn_not_mapped"
      }
      if ![_imeiEsn_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "imeiEsn_label"
          }
          on_error => "_imeiEsn_empty"
        }
      }
    }

    if [maas360Environment] != "" {
      mutate {
        replace => {
          "maas360Environment_label.key" => "maas360Environment"
          "maas360Environment_label.value" => "%{maas360Environment}"
        }
        on_error => "_maas360Environment_not_mapped"
      }
      if ![_maas360Environment_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "maas360Environment_label"
          }
          on_error => "_maas360Environment_empty"
        }
      }
    }

    if [installLocation] != "" {
      mutate {
        replace => {
          "installLocation_label.key" => "installLocation"
          "installLocation_label.value" => "%{installLocation}"
        }
        on_error => "_installLocation_not_mapped"
      }
      if ![_installLocation_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "installLocation_label"
          }
          on_error => "_installLocation_empty"
        }
      }
    }

    if [maas360ManagedStatus] != "" {
      mutate {
        replace => {
          "maas360ManagedStatus_label.key" => "maas360ManagedStatus"
          "maas360ManagedStatus_label.value" => "%{maas360ManagedStatus}"
        }
        on_error => "_maas360ManagedStatus_not_mapped"
      }
      if ![_maas360ManagedStatus_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "maas360ManagedStatus_label"
          }
          on_error => "_maas360ManagedStatus_empty"
        }
      }
    }

    if [udid] != "" {
      mutate {
        replace => {
          "udid_label.key" => "udid"
          "udid_label.value" => "%{udid}"
        }
        on_error => "_udid_not_mapped"
      }
      if ![_udid_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "udid_label"
          }
          on_error => "_udid_empty"
        }
      }
    }

    if [mailboxDeviceId] != "" {
      mutate {
        replace => {
          "mailboxDeviceId_label.key" => "mailboxDeviceId"
          "mailboxDeviceId_label.value" => "%{mailboxDeviceId}"
        }
        on_error => "_mailboxDeviceId_not_mapped"
      }
      if ![_mailboxDeviceId_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "mailboxDeviceId_label"
          }
          on_error => "_mailboxDeviceId_empty"
        }
      }
    }

    if [mailboxManaged] != "" {
      mutate {
        replace => {
          "mailboxManaged_label.key" => "mailboxManaged"
          "mailboxManaged_label.value" => "%{mailboxManaged}"
        }
        on_error => "_mailboxManaged_not_mapped"
      }
      if ![_mailboxManaged_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "mailboxManaged_label"
          }
          on_error => "_mailboxManaged_empty"
        }
      }
    }

    if [phoneNumber] != "" {
      mutate {
        replace => {
          "phoneNumber_label.key" => "phoneNumber"
          "phoneNumber_label.value" => "%{phoneNumber}"
        }
        on_error => "_phoneNumber_not_mapped"
      }
      if ![_phoneNumber_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "phoneNumber_label"
          }
          on_error => "_phoneNumber_empty"
        }
      }
    }

    if [sourceID] != "" {
      mutate {
          convert => {
              "sourceID" => "string"
          }
          on_error => "_already_string_value_sourceID"
      }
      mutate {
        replace => {
          "sourceID_label.key" => "SourceID"
          "sourceID_label.value" => "%{sourceID}"
        }
        on_error => "_sourceID_not_mapped"
      }
      if ![_sourceID_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "sourceID_label"
          }
          on_error => "_sourceID_empty"
        }
      }
    }

    if [itunesStoreAccountEnabled] != "" {
      mutate {
        replace => {
          "itunesStoreAccountEnabled_label.key" => "itunesStoreAccountEnabled"
          "itunesStoreAccountEnabled_label.value" => "%{itunesStoreAccountEnabled}"
        }
        on_error => "_itunesStoreAccountEnabled_not_mapped"
      }
      if ![_itunesStoreAccountEnabled_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "itunesStoreAccountEnabled_label"
          }
          on_error => "_itunesStoreAccountEnabled_empty"
        }
      }
    }

    if [unifiedTravelerDeviceId] != "" {
      mutate {
        replace => {
          "unifiedTravelerDeviceId_label.key" => "unifiedTravelerDeviceId"
          "unifiedTravelerDeviceId_label.value" => "%{unifiedTravelerDeviceId}"
        }
        on_error => "_unifiedTravelerDeviceId_not_mapped"
      }
      if ![_unifiedTravelerDeviceId_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "unifiedTravelerDeviceId_label"
          }
          on_error => "_unifiedTravelerDeviceId_empty"
        }
      }
    }

    if [enrollmentMode] != "" {
      mutate {
        replace => {
          "enrollmentMode_label.key" => "enrollmentMode"
          "enrollmentMode_label.value" => "%{enrollmentMode}"
        }
        on_error => "_enrollmentMode_not_mapped"
      }
      if ![_enrollmentMode_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "enrollmentMode_label"
          }
          on_error => "_enrollmentMode_empty"
        }
      }
    }

    if [platformName] != "" {
      mutate {
        replace => {
          "platformName_label.key" => "platformName"
          "platformName_label.value" => "%{platformName}"
        }
        on_error => "_platformName_not_mapped"
      }
      if ![_platformName_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "platformName_label"
          }
          on_error => "_platformName_empty"
        }
      }
    }

    if [deviceType] != "" {
      mutate {
        replace => {
          "deviceType_label.key" => "deviceType"
          "deviceType_label.value" => "%{deviceType}"
        }
        on_error => "_deviceType_not_mapped"
      }
      if ![_deviceType_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "deviceType_label"
          }
          on_error => "_deviceType_empty"
        }
      }
    }

    if [modelId] != "" {
      mutate {
        replace => {
          "modelId_label.key" => "modelId"
          "modelId_label.value" => "%{modelId}"
        }
        on_error => "_modelId_not_mapped"
      }
      if ![_modelId_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "modelId_label"
          }
          on_error => "_modelId_empty"
        }
      }
    }

    if [mdmPolicy] != "" {
      mutate {
        replace => {
          "mdmPolicy_label.key" => "mdmPolicy"
          "mdmPolicy_label.value" => "%{mdmPolicy}"
        }
        on_error => "_mdmPolicy_not_mapped"
      }
      if ![_mdmPolicy_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "mdmPolicy_label"
          }
          on_error => "_mdmPolicy_empty"
        }
      }
    }

    if [policyComplianceState] != "" {
      mutate {
        replace => {
          "policyComplianceState_label.key" => "policyComplianceState"
          "policyComplianceState_label.value" => "%{policyComplianceState}"
        }
        on_error => "_policyComplianceState_not_mapped"
      }
      if ![_policyComplianceState_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "policyComplianceState_label"
          }
          on_error => "_policyComplianceState_empty"
        }
      }
    }

    if [appComplianceState] != "" {
      mutate {
        replace => {
          "appComplianceState_label.key" => "appComplianceState"
          "appComplianceState_label.value" => "%{appComplianceState}"
        }
        on_error => "_appComplianceState_not_mapped"
      }
      if ![_appComplianceState_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "appComplianceState_label"
          }
          on_error => "_appComplianceState_empty"
        }
      }
    }

    if [ruleComplianceState] != "" {
      mutate {
        replace => {
          "ruleComplianceState_label.key" => "ruleComplianceState"
          "ruleComplianceState_label.value" => "%{ruleComplianceState}"
        }
        on_error => "_ruleComplianceState_not_mapped"
      }
      if ![_ruleComplianceState_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "ruleComplianceState_label"
          }
          on_error => "_ruleComplianceState_empty"
        }
      }
    }

    if [selectiveWipeStatus] != "" {
      mutate {
        replace => {
          "selectiveWipeStatus_label.key" => "selectiveWipeStatus"
          "selectiveWipeStatus_label.value" => "%{selectiveWipeStatus}"
        }
        on_error => "_selectiveWipeStatus_not_mapped"
      }
      if ![_selectiveWipeStatus_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "selectiveWipeStatus_label"
          }
          on_error => "_selectiveWipeStatus_empty"
        }
      }
    }

    if [jailbreakStatus] != "" {
      mutate {
        replace => {
          "jailbreakStatus_label.key" => "jailbreakStatus"
          "jailbreakStatus_label.value" => "%{jailbreakStatus}"
        }
        on_error => "_jailbreakStatus_not_mapped"
      }
      if ![_jailbreakStatus_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "jailbreakStatus_label"
          }
          on_error => "_jailbreakStatus_empty"
        }
      }
    }

    if [encryptionStatus] != "" {
      mutate {
        replace => {
          "encryptionStatus_label.key" => "encryptionStatus"
          "encryptionStatus_label.value" => "%{encryptionStatus}"
        }
        on_error => "_encryptionStatus_not_mapped"
      }
      if ![_encryptionStatus_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "encryptionStatus_label"
          }
          on_error => "_encryptionStatus_empty"
        }
      }
    }

    if [passcodeCompliance] != "" {
      mutate {
        replace => {
          "passcodeCompliance_label.key" => "passcodeCompliance"
          "passcodeCompliance_label.value" => "%{passcodeCompliance}"
        }
        on_error => "_passcodeCompliance_not_mapped"
      }
      if ![_passcodeCompliance_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "passcodeCompliance_label"
          }
          on_error => "_passcodeCompliance_empty"
        }
      }
    }

    if [mdmMailboxDeviceId] != "" {
      mutate {
        replace => {
          "mdmMailboxDeviceId_label.key" => "mdmMailboxDeviceId"
          "mdmMailboxDeviceId_label.value" => "%{mdmMailboxDeviceId}"
        }
        on_error => "_mdmMailboxDeviceId_not_mapped"
      }
      if ![_mdmMailboxDeviceId_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "mdmMailboxDeviceId_label"
          }
          on_error => "_mdmMailboxDeviceId_empty"
        }
      }
    }
    # Extract the fields with boolen values
    #if [logType] != "SoftwareInstalled" {
    if [isSupervisedDevice] != "" {
      mutate {
        convert => {
          "isSupervisedDevice" => "string"
        }
      }
      mutate {
        replace => {
          "isSupervisedDevice_label.key" => "isSupervisedDevice"
          "isSupervisedDevice_label.value" => "%{isSupervisedDevice}"
        }
        on_error => "_isSupervisedDevice_not_mapped"
      }
      if ![_isSupervisedDevice_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "isSupervisedDevice_label"
          }
          on_error => "_isSupervisedDevice_empty"
        }
      }
    }

    if [testDevice] != "" {
      mutate{
          convert => {
              "testDevice" => "string"
          }
          on_error => "_already_string_value"
      }
      mutate {
        replace => {
          "testDevice_label.key" => "testDevice"
          "testDevice_label.value" => "%{testDevice}"
        }
        on_error => "_testDevice_not_mapped"
      }
      if ![_testDevice_not_mapped] {
        mutate {
          merge => {
            "_entity.asset.labels" => "testDevice_label"
          }
          on_error => "_testDevice_empty"
        }
      }
    }
  }

  if [username] != "" {
    mutate {
      replace => {
        "_relation.entity.user.userid" => "%{username}"
        "_relation.entity_type" => "USER"
      }
    }
  }

  if [emailAddress] != "" {
    mutate {
      replace => {
        "_relation.entity_type" => "USER"
      }
    }
    mutate {
      merge => {
        "_relation.entity.user.email_addresses" => "emailAddress"
      }
    }
  }

  if [_relation] != "" {
    mutate {
      replace => {
        "_relation.relationship" => "OWNS"
      }
    }
    mutate {
      merge => {
        "event1.idm.entity.relations" => "_relation"
      }
    }
  }

  if [logType] == "DeviceDetails" {
    if [appSoftwareSize] != "" {
      mutate {
        convert => {
          "appSoftwareSize" => "string"
        }
        on_error => "_already_string_value"
      }
      mutate {
        replace => {
          "appSoftwareSize_label.key" => "appSoftwareSize"
          "appSoftwareSize_label.value.string_value" => "%{appSoftwareSize}"
        }
        on_error => "_appSoftwareSize_not_mapped"
      }
      if ![_appSoftwareSize_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "appSoftwareSize_label"
          }
          on_error => "_appSoftwareSize_empty"
        }
      }
    }

    if [appDataSize] != "" {
      mutate {
        convert => {
          "appDataSize" => "string"
        }
        on_error => "_already_string_value"
      }
      mutate {
        replace => {
          "appDataSize_label.key" => "appDataSize"
          "appDataSize_label.value.string_value" => "%{appDataSize}"
        }
        on_error => "_appDataSize_not_mapped"
      }
      if ![_appDataSize_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "appDataSize_label"
          }
          on_error => "_appDataSize_empty"
        }
      }
    }

    if [appManagedStatus] != "" {
      mutate {
        replace => {
          "appManagedStatus_label.key" => "appManagedStatus"
          "appManagedStatus_label.value.string_value" => "%{appManagedStatus}"
        }
        on_error => "_appManagedStatus_not_mapped"
      }
      if ![_appManagedStatus_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "appManagedStatus_label"
          }
          on_error => "_appManagedStatus_empty"
        }
      }
    }

    if [lastSoftwareDataRefreshDate] != "" {
      mutate {
        replace => {
          "lastSoftwareDataRefreshDate_label.key" => "lastSoftwareDataRefreshDate"
          "lastSoftwareDataRefreshDate_label.value.string_value" => "%{lastSoftwareDataRefreshDate}"
        }
        on_error => "_lastSoftwareDataRefreshDate_not_mapped"
      }
      if ![_lastSoftwareDataRefreshDate_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "lastSoftwareDataRefreshDate_label"
          }
          on_error => "_lastSoftwareDataRefreshDate_empty"
        }
      }
    }
    if [installLocation] != "" {
      mutate {
        replace => {
          "installLocation_label.key" => "installLocation"
          "installLocation_label.value.string_value" => "%{installLocation}"
        }
        on_error => "_installLocation_not_mapped"
      }
      if ![_installLocation_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "installLocation_label"
          }
          on_error => "_installLocation_empty"
        }
      }
    }
    if [maas360Environment] != "" {
      mutate {
        replace => {
          "maas360Environment_label.key" => "maas360Environment"
          "maas360Environment_label.value.string_value" => "%{maas360Environment}"
        }
        on_error => "_maas360Environment_not_mapped"
      }
      if ![_maas360Environment_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "maas360Environment_label"
          }
          on_error => "_maas360Environment_empty"
        }
      }
    }
    if [appName] != "" {
      mutate {
        replace => {
          "appName_label.key" => "appName"
          "appName_label.value.string_value" => "%{appName}"
        }
        on_error => "_appName_not_mapped"
      }
      if ![_appName_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "appName_label"
          }
          on_error => "_appName_empty"
        }
      }
    }
    if [appID] != "" {
      mutate {
        replace => {
          "appID_label.key" => "appID"
          "appID_label.value.string_value" => "%{appID}"
        }
        on_error => "_appID_not_mapped"
      }
      if ![_appID_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "appID_label"
          }
          on_error => "_appID_empty"
        }
      }
    }
    if [appVersion] != "" {
      mutate {
          convert => {
              "appVersion" => "string"
          }
          on_error => "_already_string_value"
      }
      mutate {
        replace => {
          "appVersion_label.key" => "appVersion"
          "appVersion_label.value.string_value" => "%{appVersion}"
        }
        on_error => "_appVersion_not_mapped"
      }
      if ![_appVersion_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "appVersion_label"
          }
          on_error => "_appVersion_empty"
        }
      }
    }
    if [maas360DeviceID] != "" {
      mutate {
        replace => {
          "maas360DeviceID_label.key" => "maas360DeviceID"
          "maas360DeviceID_label.value.string_value" => "%{maas360DeviceID}"
        }
        on_error => "_maas360DeviceID_not_mapped"
      }
      if ![_maas360DeviceID_not_mapped] {
        mutate {
          merge => {
            "_additional.fields" => "maas360DeviceID_label"
          }
          on_error => "_maas360DeviceID_empty"
        }
      }
    }
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.product_name" => "IBM MaaS360"
        "event.idm.read_only_udm.metadata.vendor_name" => "IBM"
        "event.idm.read_only_udm.metadata.event_type" => "GENERIC_EVENT"
        "event.idm.read_only_udm.metadata.product_event_type" => "DeviceDetails"
      }
    }
  }
  if [logType] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.product_event_type" => "%{logType}"
      }
      on_error => "logType_empty"
    }
  }
  if [unifiedTravelerDeviceId] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.resource.product_object_id" => "%{unifiedTravelerDeviceId}"
      }
      on_error => "unifiedTravelerDeviceId_empty"
    }
  }
  if [maas360DeviceID] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.hostname" => "%{maas360DeviceID}"
        "event.idm.read_only_udm.principal.asset.hostname" => "%{maas360DeviceID}"
        "has_principal" => "true"
      }
      on_error => "maas360DeviceID_empty"
    }
  }
  if [udid] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.user.userid" => "%{udid}"
      }
      on_error => "udid_empty"
    }
  }
  if [deviceType] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.resource.name" => "%{deviceType}"
      }
      on_error => "DeviceType_empty"
    }
  }
  if [osVersion] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.platform_version" => "%{osVersion}"
      }
      on_error => "osversion_empty"
    }
  }
  if [wifiMacAddress] not in ["", "N/A"] {
    mutate {
      merge => {
        "event.idm.read_only_udm.principal.mac" => "wifiMacAddress"
      }
      on_error => "wifiMacAddress_empty"
    }
  }
  if [emailAddress] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.principal.user.email_addresses" => "emailAddress"
      }
      on_error => "emailAddress_empty"
    }
  }
  if [username] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.user.user_display_name" => "%{username}"
      }
      on_error => "userDisplayName_empty"
    }
  }
  if [osName] =~ "(?i)windows" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.platform" => "WINDOWS"
      }
    }
  }
  else if [osName] =~ "(?i)Mac|iOS" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.platform" => "MAC"
      }
    }
  }
  else if [osName] =~ "(?)Lin" {
    mutate {
      replace => {
        "event.idm.read_only_udm.principal.platform" => "LINUX"
      }
    }
  }
  if [platformSerialNumber] != "" {
    mutate {
      replace => {
        "hardware.serial_number" => "%{platformSerialNumber}"
      }
      on_error => "platformSerialNumber_empty"
    }
  }
  if [manufacturer] != "" {
    mutate {
      replace => {
        "hardware.manufacturer" => "%{manufacturer}"
      }
      on_error => "manufacturer_empty"
    }
  }
  if [model] != "" {
    mutate {
      replace => {
        "hardware.model" => "%{model}"
      }
      on_error => "model_empty"
    }
  }
  mutate {
    merge => {
      "event.idm.read_only_udm.principal.asset.hardware" => "hardware"
    }
    on_error => "hardware_is_empty"
  }
  mutate {
    convert => {
      "firstRegisteredInEpochms" => "string"
    }
    on_error => "firstRegisteredInEpochms_already_string"
  }
  if [firstRegisteredInEpochms] != "" {
    mutate {
      replace => {
        "firstRegisteredInEpochms_label.value.string_value" => "%{firstRegisteredInEpochms}"
      }
      on_error => "firstRegisteredInEpochms_empty"
    }
    if ![firstRegisteredInEpochms_empty] {
      mutate {
        replace => {
          "firstRegisteredInEpochms_label.key" => "firstRegisteredInEpochms"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "firstRegisteredInEpochms_label"
        }
        on_error => "firstRegisteredInEpochms_label_empty"
      }
    }
  }
  mutate {
    convert => {
      "lastRegisteredInEpochms" => "string"
    }
    on_error => "lastRegisteredInEpochms_already_string"
  }
  if [lastRegisteredInEpochms] != "" {
    mutate {
      replace => {
        "lastRegisteredInEpochms_label.value.string_value" => "%{lastRegisteredInEpochms}"
      }
      on_error => "lastRegisteredInEpochms_empty"
    }
    if ![lastRegisteredInEpochms_empty] {
      mutate {
        replace => {
          "lastRegisteredInEpochms_label.key" => "lastRegisteredInEpochms"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "lastRegisteredInEpochms_label"
        }
        on_error => "lastRegisteredInEpochms_label_empty"
      }
    }
  }
  mutate {
    convert => {
      "lastMdmRegisteredInEpochms" => "string"
    }
    on_error => "lastMdmRegisteredInEpochms_already_string"
  }
  if [lastMdmRegisteredInEpochms] != "" {
    mutate {
      replace => {
        "lastMdmRegisteredInEpochms_label.value.string_value" => "%{lastMdmRegisteredInEpochms}"
      }
      on_error => "lastMdmRegisteredInEpochms_empty"
    }
    if ![lastMdmRegisteredInEpochms_empty] {
      mutate {
        replace => {
          "lastMdmRegisteredInEpochms_label.key" => "lastMdmRegisteredInEpochms"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "lastMdmRegisteredInEpochms_label"
        }
        on_error => "lastMdmRegisteredInEpochms_label_empty"
      }
    }
  }

  mutate {
    convert => {
      "lastReportedInEpochms" => "string"
    }
    on_error => "lastReportedInEpochms_already_string"
  }
  if [lastReportedInEpochms] != "" {
    mutate {
      replace => {
        "lastReportedInEpochms_label.value.string_value" => "%{lastReportedInEpochms}"
      }
      on_error => "lastReportedInEpochms_empty"
    }
    if ![lastReportedInEpochms_empty] {
      mutate {
        replace => {
          "lastReportedInEpochms_label.key" => "lastReportedInEpochms"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "lastReportedInEpochms_label"
        }
        on_error => "lastReportedInEpochms_label_empty"
      }
    }
  }

  mutate {
    convert => {
      "isSupervisedDevice" => "string"
    }
    on_error => "isSupervisedDevice_already_string"
  }
  if [isSupervisedDevice] != "" {
    mutate {
      replace => {
        "isSupervisedDevice_field.value.string_value" => "%{isSupervisedDevice}"
      }
      on_error => "isSupervisedDevice_empty"
    }
    if ![isSupervisedDevice_empty] {
      mutate {
        replace => {
          "isSupervisedDevice_field.key" => "isSupervisedDevice"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "isSupervisedDevice_field"
        }
        on_error => "isSupervisedDevice_field_empty"
      }
    }
  }

  mutate {
    convert => {
      "testDevice" => "string"
    }
    on_error => "testDevice_already_string"
  }
  if [testDevice] != "" {
    mutate {
      replace => {
        "testDevice_field.value.string_value" => "%{testDevice}"
      }
      on_error => "testDevice_empty"
    }
    if ![testDevice_empty] {
      mutate {
        replace => {
          "testDevice_field.key" => "testDevice"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "testDevice_field"
        }
        on_error => "testDevice_field_empty"
      }
    }
  }

  if [maas360ManagedStatus] != "" {
    mutate {
      replace => {
        "maas360ManagedStatus_field.value.string_value" => "%{maas360ManagedStatus}"
      }
      on_error => "maas360ManagedStatus_empty"
    }
    if ![maas360ManagedStatus_empty] {
      mutate {
        replace => {
          "maas360ManagedStatus_field.key" => "maas360ManagedStatus"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "maas360ManagedStatus_field"
        }
        on_error => "maas360ManagedStatus_field_empty"
      }
    }
  }

  if [itunesStoreAccountEnabled] != "" {
    mutate {
      replace => {
        "itunesStoreAccountEnabled_field.value.string_value" => "%{itunesStoreAccountEnabled}"
      }
      on_error => "itunesStoreAccountEnabled_empty"
    }
    if ![itunesStoreAccountEnabled_empty] {
      mutate {
        replace => {
          "itunesStoreAccountEnabled_field.key" => "itunesStoreAccountEnabled"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "itunesStoreAccountEnabled_field"
        }
        on_error => "itunesStoreAccountEnabled_field_empty"
      }
    }
  }

  mutate {
    convert => {
      "installedDateInEpochms" => "string"
    }
    on_error => "installedDateInEpochms_already_string"
  }
  if [installedDateInEpochms] != "" {
    mutate {
      replace => {
        "installedDateInEpochms_label.value.string_value" => "%{installedDateInEpochms}"
      }
      on_error => "installedDateInEpochms_empty"
    }
    if ![installedDateInEpochms_empty] {
      mutate {
        replace => {
          "installedDateInEpochms_label.key" => "installedDateInEpochms"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "installedDateInEpochms_label"
        }
        on_error => "installedDateInEpochms_label_empty"
      }
    }
  }
  mutate {
    convert => {
      "sourceID" => "string"
    }
    on_error => "sourceID_already_string"
  }
  if [sourceID] != "" {
    mutate {
      replace => {
        "sourceID_field.value.string_value" => "%{sourceID}"
      }
      on_error => "sourceID_empty"
    }
    if ![sourceID_empty] {
      mutate {
        replace => {
          "sourceID_field.key" => "sourceID"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "sourceID_field"
        }
        on_error => "sourceID_field_empty"
      }
    }
  }
  mutate {
    convert => {
      "imeiEsn" => "string"
    }
    on_error => "imeiEsn_already_string"
  }
  if [imeiEsn] != "" {
    mutate {
      replace => {
        "imeiEsn_field.value.string_value" => "%{imeiEsn}"
      }
      on_error => "imeiEsn_empty"
    }
    if ![imeiEsn_empty] {
      mutate {
        replace => {
          "imeiEsn_field.key" => "imeiEsn"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "imeiEsn_field"
        }
        on_error => "imeiEsn_field_empty"
      }
    }
  }

  if [installedDate] != "" {
    mutate {
      replace => {
        "installedDate_field.value.string_value" => "%{installedDate}"
      }
      on_error => "installedDate_empty"
    }
    if ![installedDate_empty] {
      mutate {
        replace => {
          "installedDate_field.key" => "installedDate"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "installedDate_field"
        }
        on_error => "installedDate_field_empty"
      }
    }
  }

  if [mailboxDeviceId] != "" {
    mutate {
      replace => {
        "mailboxDeviceId_field.value.string_value" => "%{mailboxDeviceId}"
      }
      on_error => "mailboxDeviceId_empty"
    }
    if ![mailboxDeviceId_empty] {
      mutate {
        replace => {
          "mailboxDeviceId_field.key" => "mailboxDeviceId"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "mailboxDeviceId_field"
        }
        on_error => "mailboxDeviceId_field_empty"
      }
    }
  }

  if [mailboxManaged] != "" {
    mutate {
      replace => {
        "mailboxManaged_field.value.string_value" => "%{mailboxManaged}"
      }
      on_error => "mailboxManaged_empty"
    }
    if ![mailboxManaged_empty] {
      mutate {
        replace => {
          "mailboxManaged_field.key" => "mailboxManaged"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "mailboxManaged_field"
        }
        on_error => "mailboxManaged_field_empty"
      }
    }
  }

  if [mailboxLastReportedInEpochms] != "" {
    mutate {
      replace => {
        "mailboxLastReportedInEpochms_field.value.string_value" => "%{mailboxLastReportedInEpochms}"
      }
      on_error => "mailboxLastReportedInEpochms_empty"
    }
    if ![mailboxLastReportedInEpochms_empty] {
      mutate {
        replace => {
          "mailboxLastReportedInEpochms_field.key" => "mailboxLastReportedInEpochms"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "mailboxLastReportedInEpochms_field"
        }
        on_error => "mailboxLastReportedInEpochms_field_empty"
      }
    }
  }

  if [mailboxLastReported] != "" {
    mutate {
      replace => {
        "mailboxLastReported_field.value.string_value" => "%{mailboxLastReported}"
      }
      on_error => "mailboxLastReported_empty"
    }
    if ![mailboxLastReported_empty] {
      mutate {
        replace => {
          "mailboxLastReported_field.key" => "mailboxLastReported"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "mailboxLastReported_field"
        }
        on_error => "mailboxLastReported_field_empty"
      }
    }
  }

  if [lastReported] != "" {
    mutate {
      replace => {
        "lastReported_field.value.string_value" => "%{lastReported}"
      }
      on_error => "lastReported_empty"
    }
    if ![lastReported_empty] {
      mutate {
        replace => {
          "lastReported_field.key" => "lastReported"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "lastReported_field"
        }
        on_error => "lastReported_field_empty"
      }
    }
  }

  if [policyComplianceState] != "" {
    mutate {
      replace => {
        "policyComplianceState_label_field.value.string_value" => "%{policyComplianceState}"
      }
      on_error => "policyComplianceState_empty"
    }
    if ![policyComplianceState_empty] {
      mutate {
        replace => {
          "policyComplianceState_label_field.key" => "policyComplianceState"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "policyComplianceState_label_field"
        }
        on_error => "policyComplianceState_label_empty"
      }
    }
  }
  if [userDomain] != "" {
    mutate {
      replace => {
        "userDomain_field.value.string_value" => "%{userDomain}"
      }
      on_error => "userDomain_empty"
    }
    if ![userDomain_empty] {
      mutate {
        replace => {
          "userDomain_field.key" => "userDomain"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "userDomain_field"
        }
        on_error => "userDomain_field_empty"
      }
    }
  }
  if [maas360Environment] != "" {
    mutate {
      replace => {
        "maas360Environment_field.value.string_value" => "%{maas360Environment}"
      }
      on_error => "maas360Environment_empty"
    }
    if ![maas360Environment_empty] {
      mutate {
        replace => {
          "maas360Environment_field.key" => "maas360Environment"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "maas360Environment_field"
        }
        on_error => "maas360Environment_field_empty"
      }
    }
  }
  if [encryptionStatus] != "" {
    mutate {
      replace => {
        "encryptionStatus_field.value.string_value" => "%{encryptionStatus}"
      }
      on_error => "encryptionStatus_empty"
    }
    if ![encryptionStatus_empty] {
      mutate {
        replace => {
          "encryptionStatus_field.key" => "encryptionStatus"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "encryptionStatus_field"
        }
        on_error => "encryptionStatus_field_empty"
      }
    }
  }
  if [enrollmentMode] != "" {
    mutate {
      replace => {
        "enrollmentMode_field.value.string_value" => "%{enrollmentMode}"
      }
      on_error => "enrollmentMode_empty"
    }
    if ![enrollmentMode_empty] {
      mutate {
        replace => {
          "enrollmentMode_field.key" => "enrollmentMode"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "enrollmentMode_field"
        }
        on_error => "enrollmentMode_field_empty"
      }
    }
  }
  if [deviceStatus] != "" {
    mutate {
      replace => {
        "deviceStatus_field.value.string_value" => "%{deviceStatus}"
      }
      on_error => "deviceStatus_empty"
    }
    if ![deviceStatus_empty] {
      mutate {
        replace => {
          "deviceStatus_field.key" => "deviceStatus"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "deviceStatus_field"
        }
        on_error => "deviceStatus_field_empty"
      }
    }
  }
  if [appComplianceState] != "" {
    mutate {
      replace => {
        "appComplianceState_field.value.string_value" => "%{appComplianceState}"
      }
      on_error => "appComplianceState_empty"
    }
    if ![appComplianceState_empty] {
      mutate {
        replace => {
          "appComplianceState_field.key" => "appComplianceState"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "appComplianceState_field"
        }
        on_error => "appComplianceState_field_empty"
      }
    }
  }
  if [ruleComplianceState] != "" {
    mutate {
      replace => {
        "ruleComplianceState_field.value.string_value" => "%{ruleComplianceState}"
      }
      on_error => "ruleComplianceState_empty"
    }
    if ![ruleComplianceState_empty] {
      mutate {
        replace => {
          "ruleComplianceState_field.key" => "ruleComplianceState"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "ruleComplianceState_field"
        }
        on_error => "ruleComplianceState_field_empty"
      }
    }
  }
  if [selectiveWipeStatus] != "" {
    mutate {
      replace => {
        "selectiveWipeStatus_field.value.string_value" => "%{selectiveWipeStatus}"
      }
      on_error => "selectiveWipeStatus_empty"
    }
    if ![selectiveWipeStatus_empty] {
      mutate {
        replace => {
          "selectiveWipeStatus_field.key" => "selectiveWipeStatus"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "selectiveWipeStatus_field"
        }
        on_error => "selectiveWipeStatus_field_empty"
      }
    }
  }
  if [jailbreakStatus] != "" {
    mutate {
      replace => {
        "jailbreakStatus_field.value.string_value" => "%{jailbreakStatus}"
      }
      on_error => "jailbreakStatus_empty"
    }
    if ![jailbreakStatus_empty] {
      mutate {
        replace => {
          "jailbreakStatus_field.key" => "jailbreakStatus"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "jailbreakStatus_field"
        }
        on_error => "jailbreakStatus_field_empty"
      }
    }
  }
  if [platformName] != "" {
    mutate {
      replace => {
        "platformName_field.value.string_value" => "%{platformName}"
      }
      on_error => "platformName_empty"
    }
    if ![platformName_empty] {
      mutate {
        replace => {
          "platformName_field.key" => "platformName"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "platformName_field"
        }
        on_error => "platformName_field_empty"
      }
    }
  }
  if [deviceOwner] != "" {
    mutate {
      replace => {
        "deviceOwner_field.value.string_value" => "%{deviceOwner}"
      }
      on_error => "deviceOwner_empty"
    }
    if ![deviceOwner_empty] {
      mutate {
        replace => {
          "deviceOwner_field.key" => "deviceOwner"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "deviceOwner_field"
        }
        on_error => "deviceOwner_field_empty"
      }
    }
  }
  if [ownership] != "" {
    mutate {
      replace => {
        "ownership_field.value.string_value" => "%{ownership}"
      }
      on_error => "ownership_empty"
    }
    if ![ownership_empty] {
      mutate {
        replace => {
          "ownership_field.key" => "ownership"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "ownership_field"
        }
        on_error => "ownership_field_empty"
      }
    }
  }
  if [customAssetNumber] != "" {
    mutate {
      replace => {
        "customAssetNumber_field.value.string_value" => "%{customAssetNumber}"
      }
      on_error => "customAssetNumber_empty"
    }
    if ![customAssetNumber_empty] {
      mutate {
        replace => {
          "customAssetNumber_field.key" => "customAssetNumber"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "customAssetNumber_field"
        }
        on_error => "customAssetNumber_field_empty"
      }
    }
  }
  if [deviceName] != "" {
    mutate {
      replace => {
        "deviceName_field.value.string_value" => "%{deviceName}"
      }
      on_error => "deviceName_empty"
    }
    if ![deviceName_empty] {
      mutate {
        replace => {
          "deviceName_field.key" => "deviceName"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "deviceName_field"
        }
        on_error => "deviceName_field_empty"
      }
    }
  }
  if [modelId] != "" {
    mutate {
      replace => {
        "modelId_field.value.string_value" => "%{modelId}"
      }
      on_error => "modelId_empty"
    }
    if ![modelId_empty] {
      mutate {
        replace => {
          "modelId_field.key" => "modelId"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "modelId_field"
        }
        on_error => "modelId_field_empty"
      }
    }
  }
  if [passcodeCompliance] != "" {
    mutate {
      replace => {
        "passcodeCompliance_field.value.string_value" => "%{passcodeCompliance}"
      }
      on_error => "passcodeCompliance_empty"
    }
    if ![passcodeCompliance_empty] {
      mutate {
        replace => {
          "passcodeCompliance_field.key" => "passcodeCompliance"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "passcodeCompliance_field"
        }
        on_error => "passcodeCompliance_field_empty"
      }
    }
  }
  if [mdmMailboxDeviceId] != "" {
    mutate {
      replace => {
        "mdmMailboxDeviceId_field.value.string_value" => "%{mdmMailboxDeviceId}"
      }
      on_error => "mdmMailboxDeviceId_empty"
    }
    if ![mdmMailboxDeviceId_empty] {
      mutate {
        replace => {
          "mdmMailboxDeviceId_field.key" => "mdmMailboxDeviceId"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "mdmMailboxDeviceId_field"
        }
        on_error => "mdmMailboxDeviceId_field_empty"
      }
    }
  }
  if [mdmPolicy] != "" {
    mutate {
      replace => {
        "mdmPolicy_field.value.string_value" => "%{mdmPolicy}"
      }
      on_error => "mdmPolicy_empty"
    }
    if ![mdmPolicy_empty] {
      mutate {
        replace => {
          "mdmPolicy_field.key" => "mdmPolicy"
        }
      }
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "mdmPolicy_field"
        }
        on_error => "mdmPolicy_field_empty"
      }
    }
  }
  if [appVersion] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.product_version" => "%{appVersion}"
      }
      on_error => "appVersion_empty"
    }
  }
  mutate {
    replace => {
      "event1.idm.entity.metadata.product_name" => "IBM MaaS360"
      "event1.idm.entity.metadata.vendor_name" => "IBM"
    }
  }
  if [maas360DeviceID] != "" {
    mutate {
      replace => {
        "event1.idm.entity.metadata.entity_type" => "ASSET"
      }
    }
  }
  else {
    drop {
      tag => "TAG_MALFORMED_ENCODING"
    }
  }
  mutate {
    rename => {
      "_entity" => "event1.idm.entity.entity"
    }
  }

  if [has_principal] == "true" and [has_target] == "true" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "NETWORK_CONNECTION"
      }
    }
  }
  else if [has_principal] == "true" and [has_target] == "false" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "STATUS_UPDATE"
      }
    }
  }
  else {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "GENERIC_EVENT"
      }
    }
  }

  mutate {
    merge => {
      "@output" => "event1"
    }
  }
  mutate {
    merge => {
      "@output" => "event"
    }
  }
}
