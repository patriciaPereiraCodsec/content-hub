# Product: Open Policy Agent
# Category: NA
# Supported Format: JSON
# Reference: NA
# Last Updated: 2025-01-16
# Copyright 2025 Chronicle LLC

filter {
  mutate {
    replace => {
      "event" => ""
      "principal" => ""
      "target" => ""
      "intermediary" => ""
      "security_result" => ""
      "network" => ""
      "metadata" => ""
      "additional" => ""
      "has_user" => ""
      "has_principal" => ""
      "insertId" => ""
      "logName" => ""
      "resource.labels.cluster_name" => ""
      "resource.labels.container_name" => ""
      "resource.labels.location" => ""
      "resource.labels.namespace_name" => ""
      "resource.labels.pod_name" => ""
      "resource.labels.project_id" => ""
      "resource.type" => ""
      "severity" => ""
      "timestamp" => ""
      "jsonPayload.constraint_action" => ""
      "jsonPayload.constraint_name" => ""
      "jsonPayload.constraint_api_version" => ""
      "jsonPayload.constraint_group" => ""
      "jsonPayload.constraint_kind" => ""
      "jsonPayload.constraint_namespace" => ""
      "jsonPayload.event_type" => ""
      "jsonPayload.logger" => ""
      "jsonPayload.msg" => ""
      "jsonPayload.process" => ""
      "jsonPayload.resource_api_version" => ""
      "jsonPayload.resource_group" => ""
      "jsonPayload.resource_kind" => ""
      "jsonPayload.resource_namespace" => ""
      "jsonPayload.resource_name" => ""
      "jsonPayload.ts" => ""
      "jsonPayload.details.ownerReferences_uid" => ""
      "jsonPayload.details.resource_uid" => ""
      "jsonPayload.resource_labels.app_kubernetes_io_name" => ""
      "jsonPayload.resource_labels.appID" => ""
      "jsonPayload.resource_labels.cme_project_id" => ""
      "jsonPayload.resource_labels.component" => ""
      "jsonPayload.resource_labels.component_id" => ""
      "jsonPayload.resource_labels.contact_name" => ""
      "jsonPayload.resource_labels.department_name" => ""
      "jsonPayload.resource_labels.environment" => ""
      "jsonPayload.resource_labels.flink_podtype" => ""
      "jsonPayload.resource_labels.type" => ""
      "jsonPayload.resource_labels.security.istio.io/tlsMode" => ""
      "jsonPayload.resource_labels.service.istio.io/canonical-name" => ""
      "jsonPayload.resource_labels.service.istio.io/canonical-revision" => ""
      "jsonPayload.constraint_annotations.description" => ""
      "jsonPayload.constraint_annotations.policyType" => ""
      "jsonPayload.constraint_annotations.requirement_id" => ""
      "jsonPayload.constraint_annotations.service" => ""
      "jsonPayload.constraint_annotations.title" => ""
      "jsonPayload.constraint_annotations.config.k8s.io/owning-inventory" => ""
      "jsonPayload.constraint_annotations.config.kubernetes.io/origin" => ""
    }
  }

  mutate {
    gsub => ["message","app.kubernetes.io/name","app_kubernetes_io_name"]
  }
  json {
    source => "message"
    array_function => "split_columns"
    on_error => "no_json"
  }

  if [insertId] != "" {
    mutate {
      replace => {
        "metadata.product_log_id" => "%{insertId}"
      }
      on_error => "no_insertId"
    }
  }

  if [logName] != "" {
    mutate {
      replace => {
        "logName_label.value.string_value" => "%{logName}"
        "logName_label.key" => "logName"
      }
      on_error => "no_logName"
    }
    if ![no_logName] {
      mutate {
        merge => {
          "additional.fields" => "logName_label"
        }
        on_error => "no_logName_label"
      }
    }
  }

  if [jsonPayload][event_type] != "" {
    mutate {
      replace => {
        "metadata.product_event_type" => "%{jsonPayload.event_type}"
      }
      on_error => "no_event_type"
    }
  }

  if [jsonPayload][constraint_action] != "" {
    mutate {
      replace => {
        "constraint_action_label.value.string_value" => "%{jsonPayload.constraint_action}"
        "constraint_action_label.key" => "constraint_action"
      }
      on_error => "no_constraint_action"
    }
    if ![no_constraint_action] {
      mutate {
        merge => {
          "additional.fields" => "constraint_action_label"
        }
        on_error => "no_constraint_action_label"
      }
    }
  }

  if [jsonPayload][constraint_api_version] != "" {
    mutate {
      replace => {
        "constraint_api_version_label.value.string_value" => "%{jsonPayload.constraint_api_version}"
        "constraint_api_version_label.key" => "constraint_api_version"
      }
      on_error => "no_constraint_api_version"
    }
    if ![no_constraint_api_version] {
      mutate {
        merge => {
          "additional.fields" => "constraint_api_version_label"
        }
        on_error => "no_constraint_api_version_label"
      }
    }
  }

  if [jsonPayload][constraint_group] != "" {
    mutate {
      replace => {
        "constraint_group_label.value.string_value" => "%{jsonPayload.constraint_group}"
        "constraint_group_label.key" => "constraint_group"
      }
      on_error => "no_constraint_group"
    }
    if ![no_constraint_group] {
      mutate {
        merge => {
          "additional.fields" => "constraint_group_label"
        }
        on_error => "no_constraint_group_label"
      }
    }
  }

  if [jsonPayload][constraint_kind] != "" {
    mutate {
      replace => {
        "constraint_kind_label.value.string_value" => "%{jsonPayload.constraint_kind}"
        "constraint_kind_label.key" => "constraint_kind"
      }
      on_error => "no_constraint_kind"
    }
    if ![no_constraint_kind] {
      mutate {
        merge => {
          "additional.fields" => "constraint_kind_label"
        }
        on_error => "no_constraint_kind_label"
      }
    }
  }

  if [jsonPayload][constraint_name] != "" {
    mutate {
      replace => {
        "constraint_name_label.value.string_value" => "%{jsonPayload.constraint_name}"
        "constraint_name_label.key" => "constraint_name"
      }
      on_error => "no_constraint_name"
    }
    if ![no_constraint_name] {
      mutate {
        merge => {
          "additional.fields" => "constraint_name_label"
        }
        on_error => "no_constraint_name_label"
      }
    }
  }

  if [jsonPayload][constraint_namespace] != "" {
    mutate {
      replace => {
        "constraint_namespace_label.value.string_value" => "%{jsonPayload.constraint_namespace}"
        "constraint_namespace_label.key" => "constraint_namespace"
      }
      on_error => "no_constraint_namespace"
    }
    if ![no_constraint_namespace] {
      mutate {
        merge => {
          "additional.fields" => "constraint_namespace_label"
        }
        on_error => "no_constraint_namespace_label"
      }
    }
  }

  if [jsonPayload][logger] != "" {
    mutate {
      replace => {
        "logger_label.value.string_value" => "%{jsonPayload.logger}"
        "logger_label.key" => "logger"
      }
      on_error => "no_logger"
    }
    if ![no_logger] {
      mutate {
        merge => {
          "additional.fields" => "logger_label"
        }
        on_error => "no_logger_label"
      }
    }
  }

  if [jsonPayload][msg] != "" {
    mutate {
      replace => {
        "metadata.description" => "%{jsonPayload.msg}"
      }
      on_error => "no_msg"
    }
  }

  if [jsonPayload][process] != "" {
    mutate {
      replace => {
        "process_label.value.string_value" => "%{jsonPayload.process}"
        "process_label.key" => "process"
      }
      on_error => "no_process"
    }
    if ![no_process] {
      mutate {
        merge => {
          "additional.fields" => "process_label"
        }
        on_error => "no_process_label"
      }
    }
  }

  if [jsonPayload][details][ownerReferences_uid] != "" {
    mutate {
      replace => {
        "ownerReferences_uid_label.value.string_value" => "%{jsonPayload.details.ownerReferences.uid}"
        "ownerReferences_uid_label.key" => "ownerReferences_uid"
      }
      on_error => "no_ownerReferences_uid"
    }
    if ![no_ownerReferences_uid] {
      mutate {
        merge => {
          "additional.fields" => "ownerReferences_uid_label"
        }
        on_error => "no_ownerReferences_uid_label"
      }
    }
  }

  mutate {
    replace => {
      "app_kubernetes_io_name_label.value" => "%{jsonPayload.resource_labels.app_kubernetes_io_name}"
      "app_kubernetes_io_name_label.key" => "app.kubernetes.io/name"
    }
    on_error => "no_app_kubernetes_io_name"
  }
  if ![no_app_kubernetes_io_name] and [app_kubernetes_io_name_label][value] != "" {
    mutate {
      merge => {
        "target.resource.attribute.labels" => "app_kubernetes_io_name_label"
      }
      on_error => "no_app_kubernetes_io_name_label"
    }
  }

  if [jsonPayload][resource_labels][appID] != "" {
    mutate {
      replace => {
        "appID_label.value" => "%{jsonPayload.resource_labels.appID}"
        "appID_label.key" => "appID"
      }
      on_error => "no_appID"
    }
    if ![no_appID] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "appID_label"
        }
        on_error => "no_appID_label"
      }
    }
  }

  if [jsonPayload][resource_api_version] != "" {
    mutate {
      replace => {
        "resource_api_version_label.value" => "%{jsonPayload.resource_api_version}"
        "resource_api_version_label.key" => "resource_api_version"
      }
      on_error => "no_resource_api_version"
    }
    if ![no_resource_api_version] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "resource_api_version_label"
        }
        on_error => "no_resource_api_version_label"
      }
    }
  }

  if [jsonPayload][resource_kind] != "" {
    mutate {
      replace => {
        "resource_kind_label.value" => "%{jsonPayload.resource_kind}"
        "resource_kind_label.key" => "resource_kind"
      }
      on_error => "no_resource_kind"
    }
    if ![no_resource_kind] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "resource_kind_label"
        }
        on_error => "no_resource_kind_label"
      }
    }
  }

  if [jsonPayload][resource_labels][cme_project_id] != "" {
    mutate {
      replace => {
        "cme_project_id_label.value" => "%{jsonPayload.resource_labels.cme_project_id}"
        "cme_project_id_label.key" => "cme_project_id"
      }
      on_error => "no_cme_project_id"
    }
    if ![no_cme_project_id] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "cme_project_id_label"
        }
        on_error => "no_cme_project_id_label"
      }
    }
  }

  if [jsonPayload][resource_labels][component] != "" {
    mutate {
      replace => {
        "component_label.value" => "%{jsonPayload.resource_labels.component}"
        "component_label.key" => "component"
      }
      on_error => "no_component"
    }
    if ![no_component] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "component_label"
        }
        on_error => "no_component_label"
      }
    }
  }

  if [jsonPayload][resource_labels][component_id] != "" {
    mutate {
      replace => {
        "component_id_label.value" => "%{jsonPayload.resource_labels.component_id}"
        "component_id_label.key" => "component_id"
      }
      on_error => "no_component_id"
    }
    if ![no_component_id] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "component_id_label"
        }
        on_error => "no_component_id_label"
      }
    }
  }

  if [jsonPayload][resource_labels][contact_name] != "" {
    mutate {
      replace => {
        "contact_name_label.value" => "%{jsonPayload.resource_labels.contact_name}"
        "contact_name_label.key" => "contact_name"
      }
      on_error => "no_contact_name"
    }
    if ![no_contact_name] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "contact_name_label"
        }
        on_error => "no_contact_name_label"
      }
    }
  }

  if [jsonPayload][resource_labels][department_name] != "" {
    mutate {
      replace => {
        "department_name_label.value" => "%{jsonPayload.resource_labels.department_name}"
        "department_name_label.key" => "department_name"
      }
      on_error => "no_department_name"
    }
    if ![no_department_name] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "department_name_label"
        }
        on_error => "no_department_name_label"
      }
    }
  }

  if [jsonPayload][resource_labels][environment] != "" {
    mutate {
      replace => {
        "environment_label.value" => "%{jsonPayload.resource_labels.environment}"
        "environment_label.key" => "environment"
      }
      on_error => "no_environment"
    }
    if ![no_environment] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "environment_label"
        }
        on_error => "no_environment_label"
      }
    }
  }

  if [jsonPayload][resource_labels][flink_podtype] != "" {
    mutate {
      replace => {
        "flink_podtype_label.value" => "%{jsonPayload.resource_labels.flink_podtype}"
        "flink_podtype_label.key" => "flink_podtype"
      }
      on_error => "no_flink_podtype"
    }
    if ![no_flink_podtype] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "flink_podtype_label"
        }
        on_error => "no_flink_podtype_label"
      }
    }
  }

  if [jsonPayload][resource_labels][type] != "" {
    mutate {
      replace => {
        "target.resource.resource_subtype" => "%{jsonPayload.resource_labels.type}"
      }
      on_error => "no_type"
    }
  }

  mutate {
    replace => {
      "tlsMode_label.value" => "%{jsonPayload.resource_labels.security.istio.io/tlsMode}"
      "tlsMode_label.key" => "security.istio.io/tlsMode"
    }
    on_error => "no_security_tlsmode"
  }
  if ![no_security_tlsmode] and [tlsMode_label][value] != "" {
    mutate {
      merge => {
        "target.resource.attribute.labels" => "tlsMode_label"
      }
      on_error => "no_tlsMode_label"
    }
  }

  mutate {
    replace => {
      "canonicalName_label.value" => "%{jsonPayload.resource_labels.service.istio.io/canonical-name}"
      "canonicalName_label.key" => "service.istio.io/canonical-name"
    }
    on_error => "no_canonicalName"
  }
  if ![no_canonicalName] and [canonicalName_label][value] != "" {
    mutate {
      merge => {
        "target.resource.attribute.labels" => "canonicalName_label"
        }
      on_error => "no_canonicalName_label"
    }
  }

  mutate {
    replace => {
      "canonicalRevision_label.value" => "%{jsonPayload.resource_labels.service.istio.io/canonical-revision}"
      "canonicalRevision_label.key" => "service.istio.io/canonical-revision"
    }
    on_error => "no_canonicalRevision"
  }
  if ![no_canonicalRevision] and [canonicalRevision_label][value] != "" {
    mutate {
      merge => {
        "target.resource.attribute.labels" => "canonicalRevision_label"
        }
      on_error => "no_canonicalRevision_label"
    }
  }

  if [jsonPayload][resource_namespace] != "" {
    mutate {
      replace => {
        "resource_namespace_label.value" => "%{jsonPayload.resource_namespace}"
        "resource_namespace_label.key" => "resource_namespace"
      }
      on_error => "no_resource_namespace"
    }
    if ![no_resource_namespace] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "resource_namespace_label"
        }
        on_error => "no_resource_namespace_label"
      }
    }
  }

  if [jsonPayload][resource_name] != "" {
    mutate {
      replace => {
        "target.resource.name" => "%{jsonPayload.resource_name}"
      }
      on_error => "no_resource_name"
    }
  }

  if [jsonPayload][details][resource_uid] != "" {
    mutate {
      replace => {
        "target.resource.product_object_id" => "%{jsonPayload.details.resource_uid}"
      }
      on_error => "no_resource_uid_label"
    }
  }



  if [resource][labels][cluster_name] != "" {
    mutate {
      replace => {
        "principal.resource.product_object_id" => "%{resource.labels.cluster_name}"
      }
      on_error => "no_cluster_name"
    }
  }

  if [resource][labels][container_name] != "" {
    mutate {
      replace => {
        "container_name_label.value" => "%{resource.labels.container_name}"
        "container_name_label.key" => "container_name"
      }
      on_error => "no_container_name"
    }
    if ![no_container_name] {
      mutate {
        merge => {
          "principal.resource.attribute.labels" => "container_name_label"
        }
        on_error => "no_container_name_label"
      }
    }
  }

  if [resource][labels][location] != "" {
    mutate {
      replace => {
        "principal.location.name" => "%{resource.labels.location}"
      }
      on_error => "no_location"
    }
  }

  if [resource][labels][namespace_name] != "" {
    mutate {
      replace => {
        "principal.resource.name" => "%{resource.labels.namespace_name}"
      }
      on_error => "no_namespace_name"
    }
  }

  if [resource][labels][pod_name] != "" {
    mutate {
      replace => {
        "pod_name_label.value" => "%{resource.labels.pod_name}"
        "pod_name_label.key" => "pod_name"
      }
      on_error => "no_pod_name"
    }
    if ![no_pod_name] {
      mutate {
        merge => {
          "principal.resource.attribute.labels" => "pod_name_label"
        }
        on_error => "no_pod_name_label"
      }
    }
  }

  if [resource][labels][project_id] != "" {
    mutate {
      replace => {
        "project_id_label.value" => "%{resource.labels.project_id}"
        "project_id_label.key" => "project_id"
      }
      on_error => "no_project_id"
    }
    if ![no_project_id] {
      mutate {
        merge => {
          "principal.resource.attribute.labels" => "project_id_label"
        }
        on_error => "no_project_id_label"
      }
    }
  }

  if [resource][type] != "" {
    mutate {
      replace => {
        "principal.resource.resource_subtype" => "%{resource.type}"
      }
      on_error => "no_resource_type"
    }
  }

  if [jsonPayload][constraint_annotations][description] != "" {
    mutate {
      replace => {
        "security_result.description" => "%{jsonPayload.constraint_annotations.description}"
      }
      on_error => "no_description"
    }
  }

  if [jsonPayload][constraint_annotations][title] != "" {
    mutate {
      replace => {
        "security_result.summary" => "%{jsonPayload.constraint_annotations.title}"
      }
      on_error => "no_title"
    }
  }

  if [jsonPayload][constraint_annotations][policyType] != "" {
    mutate {
      replace => {
        "policyType_label.value" => "%{jsonPayload.constraint_annotations.policyType}"
        "policyType_label.key" => "policyType"
      }
      on_error => "no_policyType"
    }
    if ![no_policyType] {
      mutate {
        merge => {
          "security_result.detection_fields" => "policyType_label"
        }
        on_error => "no_policyType_label"
      }
    }
  }

  if [jsonPayload][constraint_annotations][requirement_id] != "" {
    mutate {
      replace => {
        "requirement_id_label.value" => "%{jsonPayload.constraint_annotations.requirement_id}"
        "requirement_id_label.key" => "requirement_id"
      }
      on_error => "no_requirement_id"
    }
    if ![no_requirement_id] {
      mutate {
        merge => {
          "security_result.detection_fields" => "requirement_id_label"
        }
        on_error => "no_requirement_id_label"
      }
    }
  }

  if [jsonPayload][constraint_annotations][service] != "" {
    mutate {
      replace => {
        "service_label.value" => "%{jsonPayload.constraint_annotations.service}"
        "service_label.key" => "service"
      }
      on_error => "no_service"
    }
    if ![no_service] {
      mutate {
        merge => {
          "security_result.detection_fields" => "service_label"
        }
        on_error => "no_service_label"
      }
    }
  }

  for key,value in jsonPayload.constraint_annotations.configmanagement.gke map {
    mutate {
      replace => {
        "configmanagement_label" => ""
      }
    }
    mutate {
      replace => {
        "configmanagement_label.value" => "%{value}"
        "configmanagement_label.key" => "%{key}"
      }
      on_error => "no_configmanagement"
    }
    if ![no_configmanagement] and [value] != "" and [key] != "" {
      mutate {
        merge => {
          "security_result.detection_fields" => "configmanagement_label"
        }
        on_error => "no_configmanagement_label"
      }
    }
  }
  for key,value in jsonPayload.constraint_annotations.configsync.gke map {
    mutate {
      replace => {
        "configsync_label" => ""
      }
    }
    mutate {
      replace => {
        "configsync_label.value" => "%{value}"
        "configsync_label.key" => "%{key}"
      }
      on_error => "no_configsync"
    }
    if ![no_configsync] and [value] != "" and [key] != "" {
      mutate {
        merge => {
          "security_result.detection_fields" => "configsync_label"
        }
        on_error => "no_configsync_label"
      }
    }
  }

  mutate {
    replace => {
      "io_inventory.value" => "%{jsonPayload.constraint_annotations.config.k8s.io/owning-inventory}"
      "io_inventory.key" => "k8s io/owninginventory"
    }
    on_error => "no_io_inventory"
  }
  if ![no_io_inventory] and [io_inventory][value] != "" {
    mutate {
      merge => {
        "target.resource.attribute.labels" => "io_inventory"
      }
      on_error => "no_io_inventory_label"
    }
  }

  mutate {
    replace => {
      "kubernetes_io_origin.value" => "%{jsonPayload.constraint_annotations.config.kubernetes.io/origin}"
      "kubernetes_io_origin.key" => "kubernetes io/origin"
    }
    on_error => "no_kubernetes_io_origin"
  }
  if ![no_kubernetes_io_origin] and [kubernetes_io_origin][value] != "" {
    mutate {
      merge => {
        "target.resource.attribute.labels" => "kubernetes_io_origin"
      }
      on_error => "no_kubernetes_io_origin_label"
    }
  }

  mutate {
    uppercase => ["severity"]
  }

  if [severity] in ["INFO","INFORMATIONAL"] {
    mutate {
      replace => {
        "security_result.severity" => "INFORMATIONAL"
      }
    }
  }
  else if [severity] != "LOW" {
    mutate {
      replace => {
        "security_result.severity" => "LOW"
      }
    }
  }
  else if [severity] not in ["MEDIUM","WARNING","WARN"] {
    mutate {
      replace => {
        "security_result.severity" => "MEDIUM"
      }
    }
  }
  else if [severity] == "HIGH" {
    mutate {
      replace => {
        "security_result.severity" => "HIGH"
      }
    }
  }
  else if [severity] == "CRITICAL" {
    mutate {
      replace => {
        "security_result.severity" => "CRITICAL"
      }
    }
  }


  mutate {
    replace => {
      "metadata.vendor_name" => "Open Policy Agent"
      "metadata.product_name" => "Open Policy Agent"
    }
  }

  if [has_user] == "true" {
    mutate {
      replace => {
        "metadata.event_type" => "USER_UNCATEGORIZED"
      }
    }
  }
  else if [has_principal] == "true" {
    mutate {
      replace => {
        "metadata.event_type" => "STATUS_UPDATE"
      }
    }
  }
  else {
    mutate {
      replace => {
        "metadata.event_type" => "GENERIC_EVENT"
      }
    }
  }

  if [principal] != "" {
    mutate {
      rename => {
        "principal" => "event.idm.read_only_udm.principal"
      }
    }
  }
  if [target] != "" {
    mutate {
      rename => {
        "target" => "event.idm.read_only_udm.target"
      }
    }
  }
  if [metadata] != "" {
    mutate {
      rename => {
        "metadata" => "event.idm.read_only_udm.metadata"
      }
    }
  }
  if [network] != "" {
    mutate {
      rename => {
        "network" => "event.idm.read_only_udm.network"
      }
    }
  }
  if [additional] != "" {
    mutate {
      rename => {
        "additional" => "event.idm.read_only_udm.additional"
      }
    }
  }
  if [intermediary] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.intermediary" => "intermediary"
      }
      on_error => "no_intermediary"
    }
  }
  if [security_result] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.security_result" => "security_result"
      }
      on_error => "no_security_result"
    }
  }

  mutate {
    merge => {
      "@output" => "event"
    }
  }
}
