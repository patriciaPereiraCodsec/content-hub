# Product: VECTRA_ALERTS
# Category: Content Management Software
# Supported Format: JSON
# Last Updated: 2025-02-18
# Copyright 2025 Google SecOps

filter {
  mutate {
    replace => {
      "assigned_date" = ""
      "assigned_to" = ""
      "c_score" = ""
      "category" = ""
      "certainty" = ""
      "created_timestamp" = ""
      "custom_detection" = ""
      "data_source.connection_id" = ""
      "data_source.connection_name" = ""
      "data_source.type" = ""
      "description" = ""
      "detection" = ""
      "detection_category" = ""
      "detection_type" = ""
      "detection_url" = ""
      "filtered_by_ai" = "false"
      "filtered_by_rule" = "false"
      "filtered_by_user" = "false"
      "first_timestamp" = ""
      "grouped_details" = ""
      "groups" = ""
      "id" = ""
      "is_custom_model" = ""
      "is_marked_custom" = ""
      "is_targeting_key_asset" = ""
      "is_triaged" = ""
      "last_timestamp" = ""
      "note" = ""
      "note_modified_by" = ""
      "note_modified_timestamp" = ""
      "notes" = ""
      "sensor" = ""
      "sensor_name" = ""
      "src_account" = ""
      "src_host.certainty" = ""
      "src_host.groups" = ""
      "src_host.id" = ""
      "src_host.ip" = ""
      "src_host.is_key_asset" = ""
      "src_host.name" = ""
      "src_host.threat" = ""
      "src_host.url" = ""
      "src_ip" = ""
      "state" = ""
      "summary.num_attempts" = ""
      "summary.uuids" = ""
      "t_score" = ""
      "tags" = ""
      "targets_key_asset" = ""
      "threat" = ""
      "triage_rule_id" = ""
      "type" = ""
      "url" = ""
      "principal" = ""
      "target" = ""
      "src" = ""
      "intermediary" = ""
      "security_result" = ""
      "has_user" = "false"
      "has_principal" = "false"
      "has_target" = "false"
    }
  }
  json {
    source => "message"
    array_function => "split_columns"
    on_error => "invalid_json"
  }
  if [invalid_json] {
    mutate {
      gsub => [
        "message", 'sensor type\\"\}', 'sensor type\"'
      ]
      on_error => "invalid_json_gsub"
    }
    json {
      source => "message"
      array_function => "split_columns"
      on_error => "invalid_json1"
    }
    if [invalid_json1] {
      drop {
        tag => "TAG_MALFORMED_MESSAGE"
      }
    }
  }
  if [assigned_date] != "" {
    mutate {
      replace => {
        "assigned_date_label.key" => "Assigned Date"
        "assigned_date_label.value.string_value" => "%{assigned_date}"
      }
      on_error => "assigned_date_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "assigned_date_label"
      }
      on_error => "assigned_date_label_invalid"
    }
  }
  if [assigned_to] != "" {
    mutate {
      replace => {
        "assigned_to_label.key" => "Assigned To"
        "assigned_to_label.value.string_value" => "%{assigned_to}"
      }
      on_error => "assigned_to_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "assigned_to_label"
      }
      on_error => "assigned_to_label_invalid"
    }
  }
  if [c_score] != "" {
    mutate {
      convert => {
        "c_score" => "string"
      }
      on_error => "c_score_invalid_str"
    }
    mutate {
      replace => {
        "c_score_label.key" => "c_score"
        "c_score_label.value.string_value" => "%{c_score}"
      }
      on_error => "c_score_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "c_score_label"
      }
      on_error => "c_score_label_invalid"
    }
  }
  if [category] != "" {
    mutate {
      merge => {
        "security_result.category_details" => "category"
      }
      on_error => "category_invalid"
    }
  }
  if [certainty] != "" {
    mutate {
      convert => {
        "certainty" => "string"
      }
      on_error => "certainty_invalid_str"
    }
    mutate {
      replace => {
        "certainty_label.key" => "Certainty"
        "certainty_label.value.string_value" => "%{certainty}"
      }
      on_error => "certainty_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "certainty_label"
      }
      on_error => "certainty_label_invalid"
    }
  }
  if [created_timestamp] != "" {
    date {
      match => ["created_timestamp", "yyyy-MM-ddTHH:mm:ssZ"]
      on_error => "created_timestamp_invalid"
    }
  }
  if [custom_detection] != "" {
    mutate {
      replace => {
        "custom_detection_label.key" => "Custom Detection"
        "custom_detection_label.value.string_value" => "%{custom_detection}"
      }
      on_error => "custom_detection_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "custom_detection_label"
      }
      on_error => "custom_detection_label_invalid"
    }
  }
  if [data_source][connection_id] != "" {
    mutate {
      replace => {
        "connection_id_label.key" => "Connection ID"
        "connection_id_label.value" => "%{data_source.connection_id}"
      }
      on_error => "connection_id_invalid"
    }
    mutate {
      merge => {
        "security_result.detection_fields" => "connection_id_label"
      }
      on_error => "connection_id_label_invalid"
    }
  }
  if [data_source][connection_name] != "" {
    mutate {
      replace => {
        "connection_name_label.key" => "Connection Name"
        "connection_name_label.value" => "%{data_source.connection_name}"
      }
      on_error => "connection_name_invalid"
    }
    mutate {
      merge => {
        "security_result.detection_fields" => "connection_name_label"
      }
      on_error => "connection_name_label_invalid"
    }
  }
  if [data_source][type] != "" {
    mutate {
      replace => {
        "data_source_type_label.key" => "Data Source Type"
        "data_source_type_label.value" => "%{data_source.type}"
      }
      on_error => "data_source_type_invalid"
    }
    mutate {
      merge => {
        "security_result.detection_fields" => "data_source_type_label"
      }
      on_error => "data_source_type_label_invalid"
    }
  }
  if [description] != "" {
    mutate {
      replace => {
        "metadata.description" => "%{description}"
      }
      on_error => "description_invalid"
    }
  }
  if [detection] != "" {
    mutate {
      replace => {
        "detection_label.key" => "Detection"
        "detection_label.value.string_value" => "%{detection}"
      }
      on_error => "detection_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "detection_label"
      }
      on_error => "detection_label_invalid"
    }
  }
  if [detection_category] != "" {
    mutate {
      replace => {
        "detection_category_label.key" => "Detection Category"
        "detection_category_label.value.string_value" => "%{detection_category}"
      }
      on_error => "detection_category_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "detection_category_label"
      }
      on_error => "detection_category_label_invalid"
    }
  }
  if [detection_type] != "" {
    mutate {
      replace => {
        "detection_type_label.key" => "Detection Type"
        "detection_type_label.value.string_value" => "%{detection_type}"
      }
      on_error => "detection_type_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "detection_type_label"
      }
      on_error => "detection_type_label_invalid"
    }
  }
  if [detection_url] != "" {
    mutate {
      replace => {
        "principal.url" => "%{detection_url}"
      }
      on_error => "detection_url_invalid"
    }
  }
  if [filtered_by_ai] != "" {
    mutate {
      convert => {
        "filtered_by_ai" => "string"
      }
      on_error => "filtered_by_ai_invalid_str"
    }
    mutate {
      replace => {
        "filtered_by_ai_label.key" => "Filtered By AI"
        "filtered_by_ai_label.value.string_value" => "%{filtered_by_ai}"
      }
      on_error => "filtered_by_ai_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "filtered_by_ai_label"
      }
      on_error => "filtered_by_ai_label_invalid"
    }
  }
  if [filtered_by_rule] != "" {
    mutate {
      convert => {
        "filtered_by_rule" => "string"
      }
      on_error => "filtered_by_rule_invalid_str"
    }
    mutate {
      replace => {
        "filtered_by_rule_label.key" => "Filtered By Rule"
        "filtered_by_rule_label.value.string_value" => "%{filtered_by_rule}"
      }
      on_error => "filtered_by_rule_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "filtered_by_rule_label"
      }
      on_error => "filtered_by_rule_label_invalid"
    }
  }
  if [filtered_by_user] != "" {
    mutate {
      convert => {
        "filtered_by_user" => "string"
      }
      on_error => "filtered_by_user_invalid_str"
    }
    mutate {
      replace => {
        "filtered_by_user_label.key" => "Filtered By User"
        "filtered_by_user_label.value.string_value" => "%{filtered_by_user}"
      }
      on_error => "filtered_by_user_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "filtered_by_user_label"
      }
      on_error => "filtered_by_user_label_invalid"
    }
  }
  if [first_timestamp] != "" {
    mutate {
      replace => {
        "first_timestamp_label.key" => "First Timestamp"
        "first_timestamp_label.value.string_value" => "%{first_timestamp}"
      }
      on_error => "first_timestamp_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "first_timestamp_label"
      }
      on_error => "first_timestamp_label_invalid"
    }
  }
  for gd in grouped_details {
    mutate {
      replace => {
        "gd_first_timestamp" => ""
        "gd_last_timestamp" => ""
        "gd_grouping_field" => ""
        "gd_uuid" => ""
        "gd_src_host_ip" => ""
      }
    }
    mutate {
      replace => {
        "gd_src_host_ip" => "%{gd.src_host.ip}"
      }
      on_error => "gd_src_host_ip_invalid"
    }
    if [gd_src_host_ip] != "" {
      mutate {
        merge => {
          "principal.ip" => "gd_src_host_ip"
        }
        on_error => "gd_src_host_ip_invalid1"
      }
      if ![gd_src_host_ip_invalid1] {
        mutate {
          replace => {
            "has_principal" => "true"
          }
          on_error => "has_principal_invalid"
        }
      }
    }
    mutate {
      replace => {
        "gd_first_timestamp" => "%{gd.first_timestamp}"
      }
      on_error => "gd_first_timestamp_invalid"
    }
    mutate {
      replace => {
        "gd_last_timestamp" => "%{gd.last_timestamp}"
      }
      on_error => "gd_last_timestamp_invalid"
    }
    mutate {
      replace => {
        "gd_grouping_field" => "%{gd.grouping_field}"
      }
      on_error => "gd_grouping_field_invalid"
    }
    mutate {
      replace => {
        "gd_uuid" => "%{gd.uuid}"
      }
      on_error => "gd_uuid_invalid"
    }
    if [gd_first_timestamp] != "" {
      mutate {
        replace => {
          "gd_first_timestamp_label.key" => "first_timestamp"
          "gd_first_timestamp_label.value" => "%{gd_first_timestamp}"
        }
        on_error => "gd_first_timestamp_label_invalid"
      }
      if ![gd_first_timestamp_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "gd_first_timestamp_label"
          }
          on_error => "gd_first_timestamp_label_invalid1"
        }
      }
    }
    if [gd_last_timestamp] != "" {
      mutate {
        replace => {
          "gd_last_timestamp_label.key" => "group_details_last_timestamp"
          "gd_last_timestamp_label.value" => "%{gd_last_timestamp}"
        }
        on_error => "gd_last_timestamp_label_invalid"
      }
      if ![gd_last_timestamp_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "gd_last_timestamp_label"
          }
          on_error => "gd_last_timestamp_label_invalid1"
        }
      }
    }
    if [gd_grouping_field] != "" {
      mutate {
        replace => {
          "gd_grouping_field_label.key" => "grouping_field"
          "gd_grouping_field_label.value" => "%{gd_grouping_field}"
        }
        on_error => "gd_grouping_field_label_invalid"
      }
      if ![gd_grouping_field_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "gd_grouping_field_label"
          }
          on_error => "gd_grouping_field_label_invalid1"
        }
      }
    }
    if [gd_uuid] != "" {
      mutate {
        replace => {
          "gd_uuid_label.key" => "gd_uuid"
          "gd_uuid_label.value" => "%{gd_uuid}"
        }
        on_error => "gd_uuid_label_invalid"
      }
      if ![gd_uuid_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "gd_uuid_label"
          }
          on_error => "gd_uuid_label_invalid1"
        }
      }
    }
    for dh in gd.dst_hosts {
      mutate {
        replace => {
          "dst_port_label" => ""
          "dst_host_id_label" => ""
          "dst_host_name_label" => ""
          "sec_result" => ""
          "dst_host_ip" => ""
          "dst_host_name" => ""
          "dst_host_id" => ""
          "dst_ip1" => ""
          "dst_port1" => ""
          "dst_last_timestamp" => ""
        }
      }
      mutate {
        convert => {
          "dh.dst_host.id" => "string"
        }
        on_error => "dst_host_id_invalid_str"
      }
      mutate {
        convert => {
          "dh.dst_port" => "string"
        }
        on_error => "dst_port_invalid_str"
      }
      mutate {
        replace => {
          "dst_host_ip" => "%{dh.dst_host.ip}"
        }
        on_error => "dst_host_ip_invalid"
      }
      mutate {
        replace => {
          "dst_host_name" => "%{dh.dst_host.name}"
        }
        on_error => "dst_host_name_invalid"
      }
      mutate {
        replace => {
          "dst_host_id" => "%{dh.dst_host.id}"
        }
        on_error => "dst_host_id_invalid"
      }
      mutate {
        replace => {
          "dst_ip1" => "%{dh.dst_ip}"
        }
        on_error => "dst_ip_invalid"
      }
      mutate {
        replace => {
          "dst_port1" => "%{dh.dst_port}"
        }
        on_error => "dst_port_invalid"
      }
      mutate {
        replace => {
          "dst_last_timestamp" => "%{dh.last_timestamp}"
        }
        on_error => "dst_last_timestamp_invalid"
      }
      if [dst_host_id] != "" {
        mutate {
          replace => {
            "dst_host_id_label.key" => "dst_host_id"
            "dst_host_id_label.value" => "%{dst_host_id}"
          }
          on_error => "dst_host_id_label_invalid"
        }
        mutate {
          merge => {
            "sec_result.detection_fields" => "dst_host_id_label"
          }
          on_error => "dst_host_id_label_invalid1"
        }
      }
      if [dst_host_ip] != "" {
        mutate {
          merge => {
            "target.ip" => "dst_host_ip"
          }
          on_error => "dst_host_ip_invalid1"
        }
        if ![dst_host_ip_invalid1] {
          mutate {
            replace => {
              "has_target" => "true"
            }
            on_error => "has_target_invalid"
          }
        }
      }
      if [dst_host_name] != "" {
        mutate {
          replace => {
            "dst_host_name_label.key" => "dst_host_name"
            "dst_host_name_label.value" => "%{dst_host_name}"
          }
          on_error => "dst_host_name_label_invalid"
        }
        mutate {
          merge => {
            "sec_result.detection_fields" => "dst_host_name_label"
          }
          on_error => "dst_host_name_label_invalid1"
        }
      }
      if [dst_ip1] != "" {
        mutate {
          merge => {
            "target.ip" => "dst_ip1"
          }
          on_error => "dst_ip1_invalid"
        }
        if ![dst_ip1_invalid] {
          mutate {
            replace => {
              "has_target" => "true"
            }
            on_error => "has_target_invalid"
          }
        }
      }
      if [dst_port1] != "" {
        mutate {
          replace => {
            "dst_port_label.key" => "dst_port"
            "dst_port_label.value" => "%{dst_port1}"
          }
          on_error => "dst_port_label_invalid"
        }
        if ![dst_port_label_invalid] {
          mutate {
            merge => {
              "sec_result.detection_fields" => "dst_port_label"
            }
            on_error => "dst_port_label_invalid1"
          }
        }
      }
      if [dst_last_timestamp] != "" {
        mutate {
          replace => {
            "dst_last_timestamp_label.key" => "dst_last_timestamp"
            "dst_last_timestamp_label.value" => "%{dst_last_timestamp}"
          }
          on_error => "dst_last_timestamp_label_invalid"
        }
        mutate {
          merge => {
            "sec_result.detection_fields" => "dst_last_timestamp_label"
          }
          on_error => "dst_last_timestamp_label_invalid1"
        }
      }
      if [sec_result] != "" {
        mutate {
          merge => {
            "event.idm.read_only_udm.security_result" => "sec_result"
          }
          on_error => "security_result_invalid"
        }
      }
    }
  }
  for grp in groups {
    mutate {
      replace => {
        "grp_description" => "",
        "grp_id" => ""
        "grp_last_modified" => "",
        "grp_last_modified_by" => "",
        "grp_name" => "",
        "grp_type" => ""
        "sec_result" => ""
      }
    }
    mutate {
      replace => {
        "grp_description" => "%{grp.description}"
      }
      on_error => "grp_description_invalid"
    }
    mutate {
      convert => {
        "grp.id" => "string"
      }
      on_error => "grp_id_invalid_str"
    }
    mutate {
      replace => {
        "grp_id" => "%{grp.id}"
      }
      on_error => "grp_id_invalid"
    }
    mutate {
      replace => {
        "grp_last_modified" => "%{grp.last_modified}"
      }
      on_error => "grp_last_modified_invalid"
    }
    mutate {
      replace => {
        "grp_last_modified_by" => "%{grp.last_modified_by}"
      }
      on_error => "grp_last_modified_by_invalid"
    }
    mutate {
      replace => {
        "grp_name" => "%{grp.name}"
      }
      on_error => "grp_name_invalid"
    }
    mutate {
      replace => {
        "grp_type" => "%{grp.type}"
      }
      on_error => "grp_type_invalid"
    }
    if [grp_description] != "" {
      mutate {
        replace => {
          "grp_description_label.key" => "grp_description"
          "grp_description_label.value" => "%{grp_description}"
        }
        on_error => "grp_description_label_invalid"
      }
      if ![grp_description_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "grp_description_label"
          }
          on_error => "grp_description_label_invalid1"
        }
      }
    }
    if [grp_id] != "" {
      mutate {
        replace => {
          "grp_id_label.key" => "grp_id"
          "grp_id_label.value" => "%{grp_id}"
        }
        on_error => "grp_id_label_invalid"
      }
      if ![grp_id_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "grp_id_label"
          }
          on_error => "grp_id_label_invalid1"
        }
      }
    }
    if [grp_last_modified] != "" {
      mutate {
        replace => {
          "grp_last_modified_label.key" => "grp_last_modified"
          "grp_last_modified_label.value" => "%{grp_last_modified}"
        }
        on_error => "grp_last_modified_label_invalid"
      }
      if ![grp_last_modified_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "grp_last_modified_label"
          }
          on_error => "grp_last_modified_label_invalid1"
        }
      }
    }
    if [grp_last_modified_by] != "" {
      mutate {
        replace => {
          "grp_last_modified_by_label.key" => "grp_last_modified_by"
          "grp_last_modified_by_label.value" => "%{grp_last_modified_by}"
        }
        on_error => "grp_last_modified_by_label_invalid"
      }
      if ![grp_last_modified_by_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "grp_last_modified_by_label"
          }
          on_error => "grp_last_modified_by_label_invalid1"
        }
      }
    }
    if [grp_name] != "" {
      mutate {
        replace => {
          "grp_name_label.key" => "grp_name"
          "grp_name_label.value" => "%{grp_name}"
        }
        on_error => "grp_name_label_invalid"
      }
      if ![grp_name_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "grp_name_label"
          }
          on_error => "grp_name_label_invalid1"
        }
      }
    }
    if [grp_type] != "" {
      mutate {
        replace => {
          "grp_type_label.key" => "grp_type"
          "grp_type_label.value" => "%{grp_type}"
        }
        on_error => "grp_type_label_invalid"
      }
      if ![grp_type_label_invalid] {
        mutate {
          merge => {
            "sec_result.detection_fields" => "grp_type_label"
          }
          on_error => "grp_type_label_invalid1"
        }
      }
    }
    if [sec_result] != "" {
      mutate {
        merge => {
          "event.idm.read_only_udm.security_result" => "sec_result"
        }
        on_error => "security_result_invalid"
      }
    }
  }
  if [id] != "" {
    mutate {
      convert => {
        "id" => "string"
      }
      on_error => "id_invalid_str"
    }
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.product_log_id" => "%{id}"
      }
      on_error => "id_invalid"
    }
  }
  if [is_custom_model] != "" {
    mutate {
      convert => {
        "is_custom_model" => "string"
      }
      on_error => "is_custom_model_invalid_str"
    }
    mutate {
      replace => {
        "is_custom_model_label.key" => "is_custom_model"
        "is_custom_model_label.value.string_value" => "%{is_custom_model}"
      }
      on_error => "is_custom_model_label_invalid"
    }
    mutate {
      merge => {
        "event.idm.read_only_udm.additional.fields" => "is_custom_model_label"
      }
      on_error => "is_custom_model_label_invalid1"
    }
  }
  if [is_marked_custom] != "" {
    mutate {
      convert => {
        "is_marked_custom" => "string"
      }
      on_error => "is_marked_custom_invalid_str"
    }
    mutate {
      replace => {
        "is_marked_custom_label.key" => "is_marked_custom"
        "is_marked_custom_label.value.string_value" => "%{is_marked_custom}"
      }
      on_error => "is_marked_custom_label_invalid"
    }
    if ![is_marked_custom_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "is_marked_custom_label"
        }
        on_error => "is_marked_custom_label_invalid1"
      }
    }
  }
  if [is_targeting_key_asset] != "" {
    mutate {
      convert => {
        "is_targeting_key_asset" => "string"
      }
      on_error => "is_targeting_key_asset_invalid_str"
    }
    mutate {
      replace => {
        "is_targeting_key_asset_label.key" => "is_targeting_key_asset"
        "is_targeting_key_asset_label.value.string_value" => "%{is_targeting_key_asset}"
      }
      on_error => "is_targeting_key_asset_label_invalid"
    }
    if ![is_targeting_key_asset_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "is_targeting_key_asset_label"
        }
        on_error => "is_targeting_key_asset_label_invalid1"
      }
    }
  }
  if [is_triaged] != "" {
    mutate {
      convert => {
        "is_triaged" => "string"
      }
      on_error => "is_triaged_invalid_str"
    }
    mutate {
      replace => {
        "is_triaged_label.key" => "is_triaged"
        "is_triaged_label.value.string_value" => "%{is_triaged}"
      }
      on_error => "is_triaged_label_invalid"
    }
    if ![is_triaged_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "is_triaged_label"
        }
        on_error => "is_triaged_label_invalid1"
      }
    }
  }
  if [last_timestamp] != "" {
    mutate {
      replace => {
        "last_timestamp_label.key" => "last_timestamp"
        "last_timestamp_label.value.string_value" => "%{last_timestamp}"
      }
      on_error => "last_timestamp_label_invalid"
    }
    if ![last_timestamp_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "last_timestamp_label"
        }
        on_error => "last_timestamp_label_invalid1"
      }
    }
  }
  if [note] != "" {
    mutate {
      replace => {
        "note_label.key" => "note"
        "note_label.value.string_value" => "%{note}"
      }
      on_error => "note_label_invalid"
    }
    if ![note_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "note_label"
        }
        on_error => "note_label_invalid1"
      }
    }
  }
  if [note_modified_by] != "" {
    mutate {
      replace => {
        "note_modified_by_label.key" => "note_modified_by"
        "note_modified_by_label.value.string_value" => "%{note_modified_by}"
      }
      on_error => "note_modified_by_label_invalid"
    }
    if ![note_modified_by_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "note_modified_by_label"
        }
        on_error => "note_modified_by_label_invalid1"
      }
    }
  }
  if [note_modified_timestamp] != "" {
    mutate {
      replace => {
        "note_modified_timestamp_label.key" => "note_modified_timestamp"
        "note_modified_timestamp_label.value.string_value" => "%{note_modified_timestamp}"
      }
      on_error => "note_modified_timestamp_label_invalid"
    }
    if ![note_modified_timestamp_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "note_modified_timestamp_label"
        }
        on_error => "note_modified_timestamp_label_invalid1"
      }
    }
  }
  if [notes] != "" {
    mutate {
      replace => {
        "notes_label.key" => "notes"
        "notes_label.value.string_value" => "%{notes}"
      }
      on_error => "notes_label_invalid"
    }
    if ![notes_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "notes_label"
        }
        on_error => "notes_label_invalid1"
      }
    }
  }
  if [sensor] != "" {
    mutate {
      replace => {
        "sensor_label.key" => "sensor"
        "sensor_label.value.string_value" => "%{sensor}"
      }
      on_error => "sensor_label_invalid"
    }
    if ![sensor_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "sensor_label"
        }
        on_error => "sensor_label_invalid1"
      }
    }
  }
  if [sensor_name] != "" {
    mutate {
      replace => {
        "sensor_name_label.key" => "sensor_name"
        "sensor_name_label.value.string_value" => "%{sensor_name}"
      }
      on_error => "sensor_name_label_invalid"
    }
    if ![sensor_name_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "sensor_name_label"
        }
        on_error => "sensor_name_label_invalid1"
      }
    }
  }
  if [src_account] != "" {
    mutate {
      replace => {
        "src_account_label.key" => "src_account"
        "src_account_label.value.string_value" => "%{src_account}"
      }
      on_error => "src_account_label_invalid"
    }
    if ![src_account_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "src_account_label"
        }
        on_error => "src_account_label_invalid1"
      }
    }
  }
  if [url] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.url_back_to_product" => "%{url}"
      }
      on_error => "url_invalid"
    }
  }
  if [type] != "" {
    mutate {
      replace => {
        "type_label.key" => "type"
        "type_label.value.string_value" => "%{type}"
      }
      on_error => "type_invalid"
    }
    if ![type_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "type_label"
        }
        on_error => "type_label_invalid1"
      }
    }
  }
  if [triage_rule_id] != "" {
    mutate {
      convert => {
        "triage_rule_id" => "string"
      }
      on_error => "triage_rule_id_invalid_str"
    }
    mutate {
      replace => {
        "security_result.rule_id" => "%{triage_rule_id}"
      }
      on_error => "triage_rule_id_invalid"
    }
  }
  if [threat] != "" {
    mutate {
      convert => {
        "threat" => "string"
      }
      on_error => "threat_invalid_str"
    }
    mutate {
      replace => {
        "threat_label.key" => "threat"
        "threat_label.value" => "%{threat}"
      }
      on_error => "threat_label_invalid"
    }
    if ![threat_label_invalid] {
      mutate {
        merge => {
          "security_result.detection_fields" => "threat_label"
        }
        on_error => "threat_label_invalid1"
      }
    }
  }
  if [targets_key_asset] != "" {
    mutate {
      convert => {
        "targets_key_asset" => "string"
      }
      on_error => "targets_key_asset_invalid_str"
    }
    mutate {
      replace => {
        "targets_key_asset_label.key" => "targets_key_asset"
        "targets_key_asset_label.value.string_value" => "%{targets_key_asset}"
      }
      on_error => "targets_key_asset_label_invalid"
    }
    if ![targets_key_asset_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "targets_key_asset_label"
        }
        on_error => "targets_key_asset_label_invalid1"
      }
    }
  }
  if [tags] != "" {
    mutate {
      replace => {
        "tags_label.key" => "tags"
        "tags_label.value.string_value" => "%{tags}"
      }
      on_error => "tags_label_invalid"
    }
    if ![tags_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "tags_label"
        }
        on_error => "tags_label_invalid1"
      }
    }
  }
  if [t_score] != "" {
    mutate {
      convert => {
        "t_score" => "string"
      }
      on_error => "t_score_invalid_str"
    }
    mutate {
      replace => {
        "t_score_label.key" => "t_score"
        "t_score_label.value.string_value" => "%{t_score}"
      }
      on_error => "t_score_label_invalid"
    }
    if ![t_score_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "t_score_label"
        }
        on_error => "t_score_label_invalid1"
      }
    }
  }
  if [state] != "" {
    mutate {
      replace => {
        "state_label.key" => "state"
        "state_label.value" => "%{state}"
      }
      on_error => "state_label_invalid"
    }
    if ![state_label_invalid] {
      mutate {
        merge => {
          "security_result.detection_fields" => "state_label"
        }
        on_error => "state_label_invalid1"
      }
    }
  }
  if [src_ip] != "" {
    mutate {
      merge => {
        "principal.ip" => "src_ip"
        "principal.asset.ip" => "src_ip"
      }
      on_error => "src_ip_invalid"
    }
    if ![src_ip_invalid] {
      mutate {
        replace => {
          "has_principal" => "true"
        }
        on_error => "has_principal_invalid"
      }
    }
  }
  if [summary][num_attempts] != "" {
    mutate {
      convert => {
        "summary.num_attempts" => "string"
      }
      on_error => "num_attempts_invalid_str"
    }
    mutate {
      replace => {
        "num_attempts_label.key" => "num_attempts"
        "num_attempts_label.value.string_value" => "%{summary.num_attempts}"
      }
      on_error => "num_attempts_label_invalid"
    }
    if ![num_attempts_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "num_attempts_label"
        }
        on_error => "num_attempts_label_invalid1"
      }
    }
  }

  for index,uuid1 in summary.uuids {
    mutate {
      replace => {
        "uuid_label" => ""
        "sec_res" => ""
      }
    }
    mutate {
      replace => {
        "uuid_label.key" => "uuid"
        "uuid_label.value" => "%{uuid1}"
      }
      on_error => "uuid_label_invalid"
    }
    if ![uuid_label_invalid] {
      mutate {
        merge => {
          "sec_res.detection_fields" => "uuid_label"
        }
        on_error => "uuid_label_invalid1"
      }
    }
    if [sec_res] != "" {
      mutate {
        merge => {
          "event.idm.read_only_udm.security_result" => "sec_res"
        }
        on_error => "security_result_invalid"
      }
    }
  }
  if [src_host][certainty] != "" {
    mutate {
      convert => {
        "src_host.certainty" => "string"
      }
      on_error => "certainty_invalid_str"
    }
    mutate {
      replace => {
        "src_certainty_label.key" => "src_host_certainty"
        "src_certainty_label.value.string_value" => "%{src_host.certainty}"
      }
      on_error => "src_certainty_label_invalid"
    }
    if ![src_certainty_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "src_certainty_label"
        }
        on_error => "src_certainty_label_invalid1"
      }
    }
  }
  if [src_host][id] != "" {
    mutate {
      convert => {
        "src_host.id" => "string"
      }
      on_error => "src_host_id_invalid_str"
    }
    mutate {
      replace => {
        "principal.user.userid" => "%{src_host.id}"
      }
      on_error => "src_host_id_invalid"
    }
    if ![src_host_id_invalid] {
      mutate {
        replace => {
          "has_user" => "true"
        }
        on_error => "has_user_invalid"
      }
    }
  }
  if [src_host][ip] != "" {
    mutate {
      merge => {
        "principal.ip" => "src_host.ip"
        "principal.asset.ip" => "src_host.ip"
      }
      on_error => "src_host_ip_invalid"
    }
    if ![src_host_ip_invalid] {
      mutate {
        replace => {
          "has_principal" => "true"
        }
        on_error => "has_principal_invalid"
      }
    }
  }
  if [src_host][is_key_asset] != "" {
    mutate {
      convert => {
        "src_host.is_key_asset" => "string"
      }
      on_error => "src_host_is_key_asset_invalid_str"
    }
    mutate {
      replace => {
        "src_host_is_key_asset_label.key" => "src_host_is_key_asset"
        "src_host_is_key_asset_label.value.string_value" => "%{src_host.is_key_asset}"
      }
      on_error => "src_host_is_key_asset_label_invalid"
    }
    if ![src_host_is_key_asset_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "src_host_is_key_asset_label"
        }
        on_error => "src_host_is_key_asset_label_invalid1"
      }
    }
  }
  if [src_host][name] != "" {
    mutate {
      replace => {
        "principal.hostname" => "%{src_host.name}"
      }
      on_error => "src_host_name_invalid"
    }
    if ![src_host_name_invalid] {
      mutate {
        replace => {
          "has_principal" => "true"
        }
        on_error => "has_principal_invalid"
      }
    }
  }
  if [src_host][threat] != "" {
    mutate {
      convert => {
        "src_host.threat" => "string"
      }
      on_error => "src_host_threat_invalid_str"
    }
    mutate {
      replace => {
        "src_host_threat_label.key" => "src_host_threat"
        "src_host_threat_label.value.string_value" => "%{src_host.threat}"
      }
      on_error => "src_host_threat_label_invalid"
    }
    if ![src_host_threat_label_invalid] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "src_host_threat_label"
        }
        on_error => "src_host_threat_label_invalid1"
      }
    }
  }
  if [src_host][url] != "" {
    mutate {
      replace => {
        "src.url" => "%{src_host.url}"
      }
      on_error => "src_host_url_invalid"
    }
  }
  for group in src_host.groups {
    mutate {
      replace => {
        "sec_res" => ""
        "src_host_group_description" => ""
        "src_host_group_id" => ""
        "src_host_group_last_modified" => ""
        "src_host_group_last_modified_by" => ""
        "src_host_group_name" => ""
        "src_host_group_type" => ""
        "src_host_group_description_label" => ""
        "src_host_group_id_label" => ""
        "src_host_group_last_modified_label" => ""
        "src_host_group_last_modified_by_label" => ""
        "src_host_group_name_label" => ""
        "src_host_group_type_label" => ""
      }
    }
    mutate {
      replace => {
        "src_host_group_description" => "%{group.description}"
      }
      on_error => "src_host_group_description_invalid"
    }
    mutate {
      convert => {
        "group.id" => "string"
      }
      on_error => "src_host_group_id_invalid_str"
    }
    mutate {
      replace => {
        "src_host_group_id" => "%{group.id}"
      }
      on_error => "src_host_group_id_invalid"
    }
    mutate {
      replace => {
        "src_host_group_last_modified" => "%{group.last_modified}"
      }
      on_error => "src_host_group_last_modified_invalid"
    }
    mutate {
      replace => {
        "src_host_group_last_modified_by" => "%{group.last_modified_by}"
      }
      on_error => "src_host_group_last_modified_by_invalid"
    }
    mutate {
      replace => {
        "src_host_group_name" => "%{group.name}"
      }
      on_error => "src_host_group_name_invalid"
    }
    mutate {
      replace => {
        "src_host_group_type" => "%{group.type}"
      }
      on_error => "src_host_group_type_invalid"
    }
    if [src_host_group_description] != "" {
      mutate {
        replace => {
          "src_host_group_description_label.key" => "src_host_group_description"
          "src_host_group_description_label.value" => "%{src_host_group_description}"
        }
        on_error => "src_host_group_description_label_invalid"
      }
      if ![src_host_group_description_label_invalid] {
        mutate {
          merge => {
            "sec_res.detection_fields" => "src_host_group_description_label"
          }
          on_error => "src_host_group_description_label_invalid1"
        }
      }
    }
    if [src_host_group_id] != "" {
      mutate {
        replace => {
          "src_host_group_id_label.key" => "src_host_group_id"
          "src_host_group_id_label.value" => "%{src_host_group_id}"
        }
        on_error => "src_host_group_id_label_invalid"
      }
      if ![src_host_group_id_label_invalid] {
        mutate {
          merge => {
            "sec_res.detection_fields" => "src_host_group_id_label"
          }
          on_error => "src_host_group_id_label_invalid1"
        }
      }
    }
    if [src_host_group_last_modified] != "" {
      mutate {
        replace => {
          "src_host_group_last_modified_label.key" => "src_host_group_last_modified"
          "src_host_group_last_modified_label.value" => "%{src_host_group_last_modified}"
        }
        on_error => "src_host_group_last_modified_label_invalid"
      }
      if ![src_host_group_last_modified_label_invalid] {
        mutate {
          merge => {
            "sec_res.detection_fields" => "src_host_group_last_modified_label"
          }
          on_error => "src_host_group_last_modified_label_invalid1"
        }
      }
    }
    if [src_host_group_last_modified_by] != "" {
      mutate {
        replace => {
          "src_host_group_last_modified_by_label.key" => "src_host_group_last_modified_by"
          "src_host_group_last_modified_by_label.value" => "%{src_host_group_last_modified_by}"
        }
        on_error => "src_host_group_last_modified_by_label_invalid"
      }
      if ![src_host_group_last_modified_by_label_invalid] {
        mutate {
          merge => {
            "sec_res.detection_fields" => "src_host_group_last_modified_by_label"
          }
          on_error => "src_host_group_last_modified_by_label_invalid1"
        }
      }
    }
    if [src_host_group_name] != "" {
      mutate {
        replace => {
          "src_host_group_name_label.key" => "src_host_group_name"
          "src_host_group_name_label.value" => "%{src_host_group_name}"
        }
        on_error => "src_host_group_name_label_invalid"
      }
      if ![src_host_group_name_label_invalid] {
        mutate {
          merge => {
            "sec_res.detection_fields" => "src_host_group_name_label"
          }
          on_error => "src_host_group_name_label_invalid1"
        }
      }
    }
    if [src_host_group_type] != "" {
      mutate {
        replace => {
          "src_host_group_type_label.key" => "src_host_group_type"
          "src_host_group_type_label.value" => "%{src_host_group_type}"
        }
        on_error => "src_host_group_type_label_invalid"
      }
      if ![src_host_group_type_label_invalid] {
        mutate {
          merge => {
            "sec_res.detection_fields" => "src_host_group_type_label"
          }
          on_error => "src_host_group_type_label_invalid1"
        }
      }
    }
    if [sec_res] != "" {
      mutate {
        merge => {
          "event.idm.read_only_udm.security_result" => "sec_res"
        }
        on_error => "security_result_invalid"
      }
    }
  }
  if [principal] != "" {
    mutate {
      rename => {
        "principal" => "event.idm.read_only_udm.principal"
      }
    }
  }
  if [target] != "" {
    mutate {
      rename => {
        "target" => "event.idm.read_only_udm.target"
      }
    }
  }
  if [src] != "" {
    mutate {
      rename => {
        "src" => "event.idm.read_only_udm.src"
      }
    }
  }
  if [intermediary] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.intermediary" => "intermediary"
      }
    }
  }
  if [security_result] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.security_result" => "security_result"
    }
    on_error => "security_result_invalid"
    }
  }
  if [has_principal] == "true" and [has_target] == "true" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "NETWORK_CONNECTION"
      }
    }
  }
  else if [has_user] == "true" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "USER_UNCATEGORIZED"
      }
    }
  }
  else if [has_principal] == "true" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "STATUS_UPDATE"
      }
    }
  }
  else {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "GENERIC_EVENT"
      }
    }
  }
  mutate {
    replace => {
      "event.idm.read_only_udm.metadata.product_name" => "Vectra Alerts"
      "event.idm.read_only_udm.metadata.vendor_name" => "Vectra Alerts"
    }
  }
  mutate {
    merge => {
      "@output" => "event"
    }
  }
}
