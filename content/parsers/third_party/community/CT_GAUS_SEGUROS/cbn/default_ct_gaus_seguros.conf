# Product: GAUS SEGUROS
# Category: Alert
# Supported Format: CSV
# Reference: NA
# Last Updated: 2024-12-06
# Copyright 2024 Chronicle LLC

filter {
  mutate {
    replace => {
      "entity" => ""
      "column1" => ""
      "column2" => ""
      "column3" => ""
      "column4" => ""
      "column5" => ""
      "column6" => ""
      "column7" => ""
      "column9" => ""
      "column10" => ""
      "column11" => ""
      "column12" => ""
      "column13" => ""
      "column14" => ""
      "column15" => ""
      "column16" => ""
      "column17" => ""
      "column18" => ""
      "column19" => ""
      "column20" => ""
      "column21" => ""
      "column22" => ""
      "column23" => ""
      "column24" => ""
      "column25" => ""
      "column26" => ""
      "column27" => ""
      "column28" => ""
      "column29" => ""
      "column30" => ""
      "column31" => ""
      "column32" => ""
      "column33" => ""
      "column34" => ""
      "column35" => ""
      "column36" => ""
      "column37" => ""
      "column38" => ""
      "column39" => ""
      "has_user" => ""
    }
  }

  csv {
    source => "message"
    seperator => ","
    on_error => "no_message"
  }

  mutate {
    replace => {
      "col8" => "%{column8}"
    }
    on_error => "no_col8"
  }

  if ![no_col8] {
    if [column1] not in ["","NULL"] {
      mutate {
        replace => {
          "event.idm.read_only_udm.principal.user.userid" => "%{column1}"
          "event.idm.read_only_udm.principal.user.product_object_id" => "%{column1}"
        }
        on_error => "no_user_id"
      }
      if ![no_user_id] {
        mutate {
          replace => {
            "has_user" => "true"
          }
          on_error => "no_userid"
        }
      }
    }

    if [column2] not in ["","NULL"] {
      mutate {
        replace => {
          "event.idm.read_only_udm.principal.user.user_display_name" => "%{column2}"
        }
        on_error => "no_entity_hostname"
      }
    }

    if [column3] not in ["","NULL"] {
      mutate {
        replace => {
          "event.idm.read_only_udm.metadata.description" => "%{column3}"
        }
        on_error => "no_entity_hostname"
      }
    }

    if [column6] not in ["","NULL"] {
      mutate {
        replace => {
          "column6_label.value" => "%{column6}"
          "column6_label.key" => "UsrFecExp"
        }
        on_error => "no_column6"
      }
      if ![no_column6] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column6_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column7] not in ["","NULL"] {
      mutate {
        replace => {
          "column7_label.value" => "%{column7}"
          "column7_label.key" => "UsrEst"
        }
        on_error => "no_column7"
      }
      if ![no_column7] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column7_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column5] not in ["","NULL"] {
      if [column5] =~ "^.+@.+$" {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.email_addresses" => "column5"
          }
          on_error => "no_entity_email"
        }
      }
    }

    if [column8] not in ["","NULL"] {
      mutate {
        replace => {
          "column8_label.value" => "%{column8}"
          "column8_label.key" => "UsrBlq"
        }
        on_error => "no_column8"
      }
      if ![no_column8] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column8_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column9] not in ["","NULL"] {
      mutate {
        replace => {
          "column9_label.value" => "%{column9}"
          "column9_label.key" => "UsrFecBlq"
        }
        on_error => "no_column9"
      }
      if ![no_column9] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column9_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column10] not in ["","NULL"] {
      mutate {
        replace => {
          "column10_label.value" => "%{column10}"
          "column10_label.key" => "UsrCamPwd"
        }
        on_error => "no_column10"
      }
      if ![no_column10] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column10_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column11] not in ["","NULL"] {
      mutate {
        replace => {
          "column11_label.value" => "%{column11}"
          "column11_label.key" => "UsrPuePwd"
        }
        on_error => "no_column11"
      }
      if ![no_column11] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column11_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column12] not in ["","NULL"] {
      mutate {
        replace => {
          "column12_label.value" => "%{column12}"
          "column12_label.key" => "UsrCanSec"
        }
        on_error => "no_column12"
      }
      if ![no_column12] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column12_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column13] not in ["","NULL"] {
      mutate {
        replace => {
          "column13_label.value" => "%{column13}"
          "column13_label.key" => "UsrFecAlt"
        }
        on_error => "no_column13"
      }
      if ![no_column13] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column13_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column14] not in ["","NULL"] {
      mutate {
        replace => {
          "column14_label.value" => "%{column14}"
          "column14_label.key" => "UsrPwdFec"
        }
        on_error => "no_column14"
      }
      if ![no_column14] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column14_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column15] not in ["","NULL"] {
      mutate {
        replace => {
          "column15_label.value" => "%{column15}"
          "column15_label.key" => "UsrErrPwd"
        }
        on_error => "no_column15"
      }
      if ![no_column15] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column15_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column16] not in ["","NULL"] {
      mutate {
        replace => {
          "column16_label.value" => "%{column16}"
          "column16_label.key" => "UsrKey"
        }
        on_error => "no_column16"
      }
      if ![no_column16] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column16_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column17] not in ["","NULL"] {
      mutate {
        replace => {
          "column17_label.value" => "%{column17}"
          "column17_label.key" => "UsrGrp"
        }
        on_error => "no_column17"
      }
      if ![no_column17] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column17_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column18] not in ["","NULL"] {
      mutate {
        replace => {
          "column18_label.value" => "%{column18}"
          "column18_label.key" => "UsrFecPar"
        }
        on_error => "no_column18"
      }
      if ![no_column18] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column18_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column19] not in ["","NULL"] {
      mutate {
        replace => {
          "column19_label.value" => "%{column19}"
          "column19_label.key" => "UsrFecPar"
        }
        on_error => "no_column19"
      }
      if ![no_column19] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column19_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column20] not in ["","NULL"] {
      mutate {
        replace => {
          "column20_label.value" => "%{column20}"
          "column20_label.key" => "UsrFecPar"
        }
        on_error => "no_column20"
      }
      if ![no_column20] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column20_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column21] not in ["","NULL"] {
      mutate {
        replace => {
          "column21_label.value" => "%{column21}"
          "column21_label.key" => "UsrPerDfltExp"
        }
        on_error => "no_column21"
      }
      if ![no_column21] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column21_label"
          }
          on_error => "no_entity_label"
        }
      }
    }

    if [column22] not in ["","NULL"] {
      mutate {
        replace => {
          "column22_label.value" => "%{column22}"
          "column22_label.key" => "UsrUsrIns"
        }
        on_error => "no_column22"
      }
      if ![no_column22] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column22_label"
          }
          on_error => "no_column22_label"
        }
      }
    }

    if [column23] not in ["","NULL"] {
      mutate {
        replace => {
          "column23_label.value" => "%{column23}"
          "column23_label.key" => "UsrDatIns"
        }
        on_error => "no_column23"
      }
      if ![no_column23] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column23_label"
          }
          on_error => "no_column23_label"
        }
      }
    }

    if [column24] not in ["","NULL"] {
      mutate {
        replace => {
          "column24_label.value" => "%{column24}"
          "column24_label.key" => "UsrUsrUpd"
        }
        on_error => "no_column24"
      }
      if ![no_column24] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column24_label"
          }
          on_error => "no_column24_label"
        }
      }
    }

    if [column25] not in ["","NULL"] {
      mutate {
        replace => {
          "column25_label.value" => "%{column25}"
          "column25_label.key" => "UsrDatUpd"
        }
        on_error => "no_column25"
      }
      if ![no_column25] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column25_label"
          }
          on_error => "no_column25_label"
        }
      }
    }

    if [column26] not in ["","NULL"] {
      mutate {
        replace => {
          "column26_label.value" => "%{column26}"
          "column26_label.key" => "IntTip"
        }
        on_error => "no_column26"
      }
      if ![no_column26] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column26_label"
          }
          on_error => "no_column26_label"
        }
      }
    }

    if [column27] not in ["","NULL"] {
      mutate {
        replace => {
          "column27_label.value" => "%{column27}"
          "column27_label.key" => "Int1Cod"
        }
        on_error => "no_column27"
      }
      if ![no_column27] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column27_label"
          }
          on_error => "no_column27_label"
        }
      }
    }

    if [column28] not in ["","NULL"] {
      mutate {
        replace => {
          "column28_label.value" => "%{column28}"
          "column28_label.key" => "UsrAutSign"
        }
        on_error => "no_column28"
      }
      if ![no_column28] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column28_label"
          }
          on_error => "no_column28_label"
        }
      }
    }

    if [column29] not in ["","NULL"] {
      mutate {
        replace => {
          "column29_label.value" => "%{column29}"
          "column29_label.key" => "UsrDafCod"
        }
        on_error => "no_column29"
      }
      if ![no_column29] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column29_label"
          }
          on_error => "no_column29_label"
        }
      }
    }

    if [column30] not in ["","NULL"] {
      mutate {
        replace => {
          "column30_label.value" => "%{column30}"
          "column30_label.key" => "UsrMarP1"
        }
        on_error => "no_column30"
      }
      if ![no_column30] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column30_label"
          }
          on_error => "no_column30_label"
        }
      }
    }

    if [column31] not in ["","NULL"] {
      mutate {
        replace => {
          "column31_label.value" => "%{column31}"
          "column31_label.key" => "UsrMarP2"
        }
        on_error => "no_column31"
      }
      if ![no_column31] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column31_label"
          }
          on_error => "no_column31_label"
        }
      }
    }

    if [column32] not in ["","NULL"] {
      mutate {
        replace => {
          "column32_label.value" => "%{column32}"
          "column32_label.key" => "UsrMarP3"
        }
        on_error => "no_column32"
      }
      if ![no_column32] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column32_label"
          }
          on_error => "no_column32_label"
        }
      }
    }

    if [column33] not in ["","NULL"] {
      mutate {
        replace => {
          "column33_label.value" => "%{column33}"
          "column33_label.key" => "UsrMarP4"
        }
        on_error => "no_column33"
      }
      if ![no_column33] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column33_label"
          }
          on_error => "no_column33_label"
        }
      }
    }

    if [column34] not in ["","NULL"] {
      mutate {
        replace => {
          "column34_label.value" => "%{column34}"
          "column34_label.key" => "UsrMarP5"
        }
        on_error => "no_column34"
      }
      if ![no_column34] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column34_label"
          }
          on_error => "no_column34_label"
        }
      }
    }

    if [column35] not in ["","NULL"] {
      mutate {
        replace => {
          "column35_label.value" => "%{column35}"
          "column35_label.key" => "UsrValLDAP"
        }
        on_error => "no_column35"
      }
      if ![no_column35] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column35_label"
          }
          on_error => "no_column35_label"
        }
      }
    }

    if [column36] not in ["","NULL"] {
      mutate {
        replace => {
          "column36_label.value" => "%{column36}"
          "column36_label.key" => "UsrPermParPol"
        }
        on_error => "no_column36"
      }
      if ![no_column36] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column36_label"
          }
          on_error => "no_column36_label"
        }
      }
    }

    if [column37] not in ["","NULL"] {
      mutate {
        replace => {
          "column37_label.value" => "%{column37}"
          "column37_label.key" => "UsrDafAllNomb"
        }
        on_error => "no_column37"
      }
      if ![no_column37] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column37_label"
          }
          on_error => "no_column37_label"
        }
      }
    }

    if [column38] not in ["","NULL"] {
      mutate {
        replace => {
          "column38_label.value" => "%{column38}"
          "column38_label.key" => "UsrFecBaja"
        }
        on_error => "no_column38"
      }
      if ![no_column38] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column38_label"
          }
          on_error => "no_column38_label"
        }
      }
    }

    if [column39] not in ["","NULL"] {
      mutate {
        replace => {
          "column39_label.value" => "%{column39}"
          "column39_label.key" => "UsrMarcaBaja"
        }
        on_error => "no_column39"
      }
      if ![no_column39] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "column39_label"
          }
          on_error => "no_column39_label"
        }
      }
    }
  }
  else {
    mutate {
      gsub => ['message','\\\"','']
    }
    mutate {
      gsub => ['message',';',',']
    }
    csv {
      source => "message"
      seperator => ","
      # on_error => "csv_failed"
    }
    if [column3] not in ["","NULL"] {
      mutate {
        replace => {
          "event.idm.read_only_udm.principal.user.product_object_id" => "%{column3}"
        }
        on_error => "no_entity_hostname"
      }
    }
    if [column4] not in ["","NULL"] {
      mutate {
        replace => {
          "event.idm.read_only_udm.principal.user.userid" => "%{column4}"
        }
        on_error => "no_entity_userid"
      }
      if ![no_entity_userid] {
        mutate {
          replace => {
            "has_user" => "true"
          }
          on_error => "no_userid"
        }
      }
    }
    if [column5] != "" {
      mutate {
        replace => {
          "type_label.value" => "%{column5}"
          "type_label.key" => "type"
        }
        on_error => "no_type_label"
      }
      if ![no_type_label] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.user.attribute.labels" => "type_label"
          }
          on_error => "no_type_label"
        }
      }
    }
    if [column7] not in ["","NULL"] {
      mutate {
        replace => {
          "event.idm.read_only_udm.metadata.description" => "%{column7}"
        }
        on_error => "no_entity_hostname"
      }
    }
  }

  mutate {
    replace => {
      "event.idm.read_only_udm.metadata.vendor_name" => "Colinet Trotta"
      "event.idm.read_only_udm.metadata.product_name" => "GAUS SEGUROS"
    }
  }
  if [has_user] == "true" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "USER_UNCATEGORIZED"
      }
    }
  }
  else {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "GENERIC_EVENT"
      }
    }
  }

  if [entity] != "" {
    mutate {
      rename => {
        "entity" => "event.idm.entity"
      }
    }
  }


  # Output the mapped fields as an UDM event
  mutate {
    merge => {
      "@output" => "event"
    }
  }
}
