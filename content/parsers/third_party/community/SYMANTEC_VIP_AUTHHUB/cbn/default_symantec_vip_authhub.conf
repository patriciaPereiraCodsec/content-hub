# Product: SYMANTEC_VIP_AUTHHUB
# Category: VPN
# Supported Format: JSON
# References: NA
# Last Updated: 2025-03-11
# Copyright 2025 Google SecOps

filter {
  mutate {
    replace => {
      "clientIp" => ""
      "accessTokenTid" => ""
      "api" => ""
      "accessTokenScopes" => ""
      "appId" => ""
      "appName" => ""
      "azpName" => ""
      "countryISO" => ""
      "eventId" => ""
      "flowStateId" => ""
      "guid" => ""
      "host" => ""
      "geo.city_name" => ""
      "geo.country_name" => ""
      "geo.location.lat" => ""
      "geo.location.lon" => ""
      "httpMethod" => ""
      "userAgent" => ""
      "responseCode" => ""
      "userLoginId" => ""
      "userLoginIdAttributeMappingName" => ""
      "kubernetes.annotations.cni.projectcalico.org_containerID" => ""
      "kubernetes.container_hash" => ""
      "kubernetes.container_image" => ""
      "kubernetes.container_name" => ""
      "kubernetes.docker_id" => ""
      "kubernetes.namespace_name" => ""
      "kubernetes.pod_id" => ""
      "kubernetes.pod_name" => ""
      "internal-user-sync-ext-resourceGuid" => ""
      "internal-user-sync-ext-resourceName" => ""
      "auth_type" => ""
      "clientId" => ""
      "timestamp" => ""
      "msg" => ""
      "userIP" => ""
      "ip2" => ""
      "ip3" => ""
      "ip4" => ""
      "stream" => ""
      "logtag" => ""
      "type" => ""
      "remoteAddr" => ""
      "kv_data" => ""
      "requestId" => ""
      "status" => ""
      "vhost" => ""
      "remoteUser" => ""
      "contentType" => ""
      "clientTxnId" => ""
      "txnId" => ""
      "log" => ""
      "client" => ""
      "level" => ""
      "kubernetes.host" => ""
      "bytes_sent" => ""
      "requestTime" => ""
      "protocol" => ""
      "path" => ""
      "reqLength" => ""
      "method" => ""
      "httpReferrer" => ""
      "kubernetes.labels.io_component" => ""
      "kubernetes.labels.io_instance" => ""
      "kubernetes.labels.io_managed-by" => ""
      "kubernetes.labels.io_name" => ""
      "kubernetes.labels.io_part-of" => ""
      "kubernetes.labels.io_version" => ""
      "sh_chart" => ""
      "pod-template-hash" => ""
      "pod-ip" => ""
      "pod-ips" => ""
      "clientTid" => ""
      "identitySourceId" => ""
      "issuerUrl" => ""
      "imageTag" => ""
      "thread" => ""
      "request" => ""
      "referrer" => ""
      "server" => ""
      "__isAuditIdLcmIdStore" => ""
      "principalId" => ""
      "principalType" => ""
      "relVersion" => ""
      "service" => ""
      "sub" => ""
      "subType" => ""
      "tid" => ""
      "tname" => ""
      "userDN" => ""
      "userGuid" => ""
      "userIdpGuid" => ""
      "userRiskLevel" => ""
      "userRiskScore" => ""
      "userIp" => ""
      "userUniversalId" => ""
      "has_principal" => "false"
      "has_target" => "false"
      "network" => ""
      "additional" => ""
      "principal" => ""
      "target" => ""
      "metadata" => ""
      "security_result" => ""
      "intermediary" => ""
      "azserver-idp-response-verify-cert-ext-idpId" => ""
      "__THIS_RESOURCE_ID" => ""
      "admin-identityprovider-create-ext-resourceGuid" => ""
    }
  }
  mutate {
    gsub => ["message", "event.q", "event1."]
  }
  mutate {
    gsub => ["message", "org/", "org_"]
  }
  mutate {
    gsub => ["message", "helm.sh/chart", "helm.sh_chart"]
  }
  mutate {
    gsub => ["message", "app.kubernetes.io/component", "io_component"]
  }
  mutate {
    gsub => ["message", "app.kubernetes.io/instance", "io_instance"]
  }
  mutate {
    gsub => ["message", "app.kubernetes.io/managed-by", "io_managed-by"]
  }
  mutate {
    gsub => ["message", "app.kubernetes.io/name", "io_name"]
  }
  mutate {
    gsub => ["message", "app.kubernetes.io/part-of", "io_part-of"]
  }
  mutate {
    gsub => ["message", "app.kubernetes.io/version", "io_version"]
  }
  json {
    source => "message"
    array_function => "split_columns"
    on_error => "not_json"
  }
  if [not_json] {
    drop {
      tag => "TAG_UNSUPPORTED"
    }
  }
  if [kubernetes][labels][io_component] != "" {
    mutate {
      replace => {
        "io_component_label.key" => "io_component"
        "io_component_label.value" => "%{kubernetes.labels.io_component}"
      }
      on_error => "no_io_component"
    }
    if ![no_io_component] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "io_component_label"
        }
        on_error => "no_io_component_merge_error"
      }
    }
  }
  if [kubernetes][labels][io_instance] != "" {
    mutate {
      replace => {
        "io_instance_label.key" => "io_instance"
        "io_instance_label.value" => "%{kubernetes.labels.io_instance}"
      }
      on_error => "no_io_instance"
    }
    if ![no_io_instance] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "io_instance_label"
        }
        on_error => "no_io_instance_"
      }
    }
  }
  if [kubernetes][labels][io_managed-by] != "" {
    mutate {
      replace => {
        "io_managed-by_label.key" => "io_managed-by"
        "io_managed-by_label.value" => "%{kubernetes.labels.io_managed-by}"
      }
      on_error => "no_io_managed-by"
    }
    if ![no_io_managed-by] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "io_managed-by_label"
        }
        on_error => "no_io_managed-by_merge_error"
      }
    }
  }
  if [kubernetes][labels][io_name] != "" {
    mutate {
      replace => {
        "io_name_label.key" => "io_name"
        "io_name_label.value" => "%{kubernetes.labels.io_name}"
      }
      on_error => "no_io_name"
    }
    if ![no_io_name] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "io_name_label"
        }
        on_error => "no_io_name_merge_error"
      }
    }
  }
  if [kubernetes][labels][io_part-of] != "" {
    mutate {
      replace => {
        "io_part-of_label.key" => "io_part-of"
        "io_part-of_label.value" => "%{kubernetes.labels.io_part-of}"
      }
      on_error => "no_io_part-of"
    }
    if ![no_io_part-of] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "io_part-of_label"
        }
        on_error => "no_io_part-of_merge_error"
      }
    }
  }
  if [kubernetes][labels][io_version] != "" {
    mutate {
      replace => {
        "io_version_label.key" => "io_version"
        "io_version_label.value" => "%{kubernetes.labels.io_version}"
      }
      on_error => "no_io_version"
    }
    if ![no_io_version] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "io_version_label"
        }
        on_error => "no_io_version_merge_error"
      }
    }
  }
  if [log] != "" {
    grok {
      match => {
        "log" => [
          "\\[%{DATA:level}\\] %{DATA}, %{GREEDYDATA:kv_data}"
        ]
      }
      overwrite => ["level", "kv_data"]
      on_error => "no_log"
    }
  }
  if [kv_data] != "" {
    mutate {
      gsub => ["kv_data", ", ", ","]
    }
    mutate {
      gsub => ["kv_data", ": ", ":"]
    }
    kv {
      source => "kv_data"
      field_split => ","
      value_split => ":"
      on_error => "no_kv_data"
    }
  }
  if [client] != "" {
    grok {
      match => {
        "client" => [
          "%{IP:ip1}"
        ]
      }
      overwrite => ["ip1"]
      on_error => "no_client"
    }
    if ![no_client] {
      mutate {
        merge => {
          "principal.ip" => "client"
          "principal.asset.ip" => "client"
        }
        on_error => "no_client_merge_error"
      }
      mutate {
        replace => {
          "has_principal" => "true"
        }
      }
    }
  }
  if [request] != "" {
    mutate {
      gsub => ["request", '\\\"', ""]
    }
    grok {
      match => {
        "request" => [
          "%{WORD:method} %{DATA:path} %{WORD:protocol}"
        ]
      }
      overwrite => ["method","path","protocol"]
      on_error => "no_request"
    }
  }
  if [referrer] != "" {
    mutate {
      gsub => ["referrer", '\\\"', ""]
    }
    mutate {
      replace => {
        "network.http.referral_url" => "%{referrer}"
      }
      on_error => "no_referrer"
    }
  }
  if [server] != "" {
    mutate {
      replace => {
        "target.hostname" => "%{server}"
        "target.asset.hostname" => "%{server}"
        "has_target" => "true"
      }
      on_error => "no_server"
    }
  }
  if [method] != "" {
    mutate {
      replace => {
        "network.http.method" => "%{method}"
      }
      on_error => "no_method"
    }
  }
  if [httpReferrer] != "" {
    mutate {
      replace => {
        "network.http.referral_url" => "%{httpReferrer}"
      }
      on_error => "no_http_referrer"
    }
  }
  if [bytes_sent] != "" {
    mutate {
      replace => {
        "network.sent_bytes" => "%{bytes_sent}"
      }
      on_error => "no_bytes_sent"
    }
    mutate {
      convert => {
        "network.sent_bytes" => "uinteger"
      }
      on_error => "conversion_failed"
    }
  }
  if [status] not in ["", "-"] {
    mutate {
      replace => {
        "network.http.response_code" => "%{status}"
      }
      on_error => "no_status"
    }
    mutate {
      convert => {
        "network.http.response_code" => "integer"
      }
      on_error => "conversion_failed"
    }
  }
  if [path] != "" {
    mutate {
      replace => {
        "principal.file.full_path" => "%{path}"
      }
      on_error => "no_path"
    }
  }
  if [protocol] =~ "(?i)HTTP" {
    mutate {
      replace => {
        "network.application_protocol" => "HTTP"
      }
    }
  }
  if [userIP] != "" {
    grok {
      match => {
        "userIP" => [
          "%{IP:ip2}, %{IP:ip3}, %{IP:ip4}",
        ]
      }
      overwrite => ["ip2", "ip3", "ip4"]
      on_error => "no_user_ip"
    }
    if ![no_user_ip] {
      if [ip2] != "" {
        mutate {
          merge => {
            "principal.ip" => "ip2"
            "principal.asset.ip" => "ip2"
          }
          on_error => "no_user_ip_merge_error"
        }
        mutate {
          replace => {
            "has_principal" => "true"
          }
        }
      }
      if [ip3] != "" {
        mutate {
          merge => {
            "target.ip" => "ip3"
            "target.asset.ip" => "ip3"
          }
          on_error => "no_target_ip"
        }
        mutate {
          replace => {
            "has_target" => "true"
          }
        }
      }
      if [ip4] != "" {
        mutate {
          replace => {
            "intermediary_Ip" => "%{ip4}"
          }
          on_error => "no_intermediary_ip"
        }
        if [intermediary_Ip] != "" {
          mutate {
            merge => {
              "intermediary.ip" => "intermediary_Ip"
            }
            on_error => "no_intermediary_ip_merge_error"
          }
        }
      }
    }
  }
  if [level] =~ "(?i)(notice|info)" {
    mutate {
      replace => {
        "security_result.severity" => "INFORMATIONAL"
      }
    }
  }
  if [timestamp] != "" {
    date {
      match => ["timestamp", "ISO8601"]
      on_error => "invalid_date"
    }
  }
  if [kubernetes][annotations][cni][projectcalico][org_containerID] != "" {
    mutate {
      replace => {
        "target.resource.product_object_id" => "%{kubernetes.annotations.cni.projectcalico.org_containerID}"
      }
      on_error => "no_container_id"
    }
  }
  if [clientId] != "" {
    mutate {
      replace => {
        "principal.user.userid" => "%{clientId}"
      }
      on_error => "no_client_id"
    }
  }
  else {
    if [principalId] != "clientId" and [principalId] != "" {
      mutate {
        replace => {
          "principal.user.userid" => "%{principalId}"
        }
        on_error => "no_principal_id"
      }
    }
  }
  if [internal-user-sync-ext-resourceGuid] != "" {
    mutate {
      replace => {
        "target.user.userid" => "%{internal-user-sync-ext-resourceGuid}"
      }
      on_error => "no_internal_user_sync_ext_resource_guid"
    }
  }
  if [internal-user-sync-ext-resourceName] != "" and [internal-user-sync-ext-resourceName] =~ "^.+@.+$" {
    mutate {
      merge => {
        "target.user.email_addresses" => "internal-user-sync-ext-resourceName"
      }
      on_error => "no_internal_user_sync_ext_resource_name"
    }
  }
  if [countryISO] != "" {
    mutate {
      replace => {
        "principal.location.country_or_region" => "%{countryISO}"
      }
      on_error => "no_country_iso"
    }
  }
  if [geo][city_name] != "" {
    mutate {
      replace => {
        "principal.location.city" => "%{geo.city_name}"
      }
      on_error => "no_city_name"
    }
  }
  if [geo][country_name] != "" {
    mutate {
      replace => {
        "principal.location.country_or_region" => "%{geo.country_name}"
      }
      on_error => "no_country_name"
    }
  }
  if [geo][location][lat] not in ["", "UNKNOWN","null"] {
    mutate {
      convert => {
        "geo.location.lat" => "float"
      }
      on_error => "no_geo_location_lat"
    }
    mutate {
      rename => {
        "geo.location.lat" => "principal.location.region_coordinates.latitude"
      }
    }
  }
  if [geo][location][lon] not in ["", "UNKNOWN","null"] {
    mutate {
      convert => {
        "geo.location.lon" => "float"
      }
      on_error => "no_geo_location_long"
    }
    mutate {
      rename => {
        "geo.location.lon" => "principal.location.region_coordinates.longitude"
      }
    }
  }
  if [eventId] != "" {
    mutate {
      replace => {
        "metadata.product_event_type" => "%{eventId}"
      }
      on_error => "no_event_id"
    }
  }
  if [clientIp] != "" {
    mutate {
      merge => {
        "principal.ip" => "clientIp"
        "principal.asset.ip" => "clientIp"
      }
      on_error => "no_client_ip"
    }
    mutate {
      replace => {
        "has_principal" => "true"
      }
    }
  }
  if [kubernetes][host] != "" {
    mutate {
      replace => {
        "principal.hostname" => "%{kubernetes.host}"
        "principal.asset.hostname" => "%{kubernetes.host}"
        "has_principal" => "true"
      }
      on_error => "no_host"
    }
  }
  if [host] != "" {
    mutate {
      gsub => ["host", '\\\"', ""]
    }
    mutate {
      replace => {
        "principal.hostname" => "%{host}"
        "principal.asset.hostname" => "%{host}"
        "has_principal" => "true"
      }
      on_error => "no_host"
    }
  }
  if [httpMethod] != "" {
    mutate {
      replace => {
        "network.http.method" => "%{httpMethod}"
      }
      on_error => "no_http_method"
    }
  }
  if [userAgent] != "" {
    mutate {
      replace => {
        "network.http.user_agent" => "%{userAgent}"
        "network.http.parsed_user_agent" => "%{userAgent}"
      }
      on_error => "no_user_agent"
    }
    mutate {
      convert => {
        "network.http.parsed_user_agent" => "parseduseragent"
      }
      on_error => "useragent_not_available"
    }
  }
  if [responseCode] != "" {
    grok {
      match => {
        "responseCode" => "%{NUMBER:responseCode}"
      }
      overwrite => ["responseCode"]
      on_error => "no_response_code"
    }
    mutate {
      replace => {
        "network.http.response_code" => "%{responseCode}"
      }
      on_error => "no_response_code"
    }
    mutate {
      convert => {
        "network.http.response_code" => "integer"
      }
      on_error => "no_response_code"
    }
  }
  if [userLoginId] != "" and [userLoginId] =~ "^.+@.+$" {
    mutate {
      merge => {
        "target.user.email_addresses" => "userLoginId"
      }
      on_error => "no_user_login_id"
    }
  }
  if [userLoginIdAttributeMappingName] != "" {
    mutate {
      replace => {
        "target.user.user_display_name" => "%{userLoginIdAttributeMappingName}"
      }
      on_error => "no_user_login_id_attribute_mapping_name"
    }
  }
  if [guid] != "" {
    mutate {
      replace => {
        "metadata.product_log_id" => "%{guid}"
      }
      on_error => "no_guid"
    }
  }
  if [appName] != "" {
    mutate {
      replace => {
        "principal.application" => "%{appName}"
      }
      on_error => "no_app_name"
    }
  }
  if [appId] != "" {
    mutate {
      replace => {
        "appIdlabel.key" => "appId"
        "appIdlabel.value.string_value" => "%{appId}"
      }
      on_error => "no_app_id"
    }
    if ![no_app_id] {
      mutate {
        merge => {
          "additional.fields" => "appIdlabel"
        }
        on_error => "no_app_id_merge_error"
      }
    }
  }
  if [relVersion] != "" {
    mutate {
      replace => {
        "metadata.product_version" => "%{relVersion}"
      }
      on_error => "no_rel_version"
    }
  }
  if [thread] != "" {
    mutate {
      replace => {
        "threadlabel.key" => "thread"
        "threadlabel.value.string_value" => "%{thread}"
      }
      on_error => "no_thread"
    }
    if ![no_thread] {
      mutate {
        merge => {
          "additional.fields" => "threadlabel"
        }
        on_error => "no_thread_merge_error"
      }
    }
  }
  if [principalId] != "" {
    mutate {
      replace => {
        "principalIdlabel.key" => "principalId"
        "principalIdlabel.value.string_value" => "%{principalId}"
      }
      on_error => "no_principal_id"
    }
    if ![no_principal_id] {
      mutate {
        merge => {
          "additional.fields" => "principalIdlabel"
        }
        on_error => "no_principal_id_merge_error"
      }
    }
  }
  if [service] != "" {
    mutate {
      replace => {
        "serviceLabel.key" => "service"
        "serviceLabel.value.string_value" => "%{service}"
      }
      on_error => "no_service"
    }
    if ![no_service] {
      mutate {
        merge => {
          "additional.fields" => "serviceLabel"
        }
        on_error => "no_service_merge_error"
      }
    }
  }
  if [sub] != "" {
    mutate {
      replace => {
        "sublabel.key" => "sub"
        "sublabel.value.string_value" => "%{sub}"
      }
      on_error => "no_sub"
    }
    if ![no_sub] {
      mutate {
        merge => {
          "additional.fields" => "sublabel"
        }
        on_error => "no_sub_merge_error"
      }
    }
  }
  if [subType] != "" {
    mutate {
      replace => {
        "subTypeLabel.key" => "subType"
        "subTypeLabel.value.string_value" => "%{subType}"
      }
      on_error => "no_sub_type"
    }
    if ![no_sub_type] {
      mutate {
        merge => {
          "additional.fields" => "subTypeLabel"
        }
        on_error => "no_sub_type_merge_error"
      }
    }
  }
  if [tid] != "" {
    mutate {
      replace => {
        "tidlabel.key" => "tid"
        "tidlabel.value.string_value" => "%{tid}"
      }
      on_error => "no_tid"
    }
    if ![no_tid] {
      mutate {
        merge => {
          "additional.fields" => "tidlabel"
        }
        on_error => "no_tid_merge_error"
      }
    }
  }
  if [tname] != "" {
    mutate {
      replace => {
        "tnamelabel.key" => "tname"
        "tnamelabel.value.string_value" => "%{tname}"
      }
      on_error => "no_tname"
    }
    if ![no_tname] {
      mutate {
        merge => {
          "additional.fields" => "tnamelabel"
        }
        on_error => "no_tname_merge_error"
      }
    }
  }
  if [userDN] != "" {
    mutate {
      replace => {
        "userDNlabel.key" => "userDN"
        "userDNlabel.value.string_value" => "%{userDN}"
      }
      on_error => "no_user_dn"
    }
    if ![no_user_dn] {
      mutate {
        merge => {
          "additional.fields" => "userDNlabel"
        }
        on_error => "no_user_dn_merge_error"
      }
    }
  }
  if [userGuid] not in ["", " "] {
    mutate {
      replace => {
        "userGuidlabel.key" => "userGuid"
        "userGuidlabel.value.string_value" => "%{userGuid}"
      }
      on_error => "no_user_guid"
    }
    if ![no_user_guid] {
      mutate {
        merge => {
          "additional.fields" => "userGuidlabel"
        }
        on_error => "no_user_guid_merge_error"
      }
    }
  }
  if [userIdpGuid] != "" {
    mutate {
      replace => {
        "userIdpGuidlabel.key" => "userIdpGuid"
        "userIdpGuidlabel.value.string_value" => "%{userIdpGuid}"
      }
      on_error => "no_user_idp_guid"
    }
    if ![no_user_idp_guid] {
      mutate {
        merge => {
          "additional.fields" => "userIdpGuidlabel"
        }
        on_error => "no_user_idp_guid_merge_error"
      }
    }
  }
  if [userRiskLevel] not in ["", " "] {
    mutate {
      replace => {
        "userRiskLevellabel.key" => "userRiskLevel"
        "userRiskLevellabel.value.string_value" => "%{userRiskLevel}"
      }
      on_error => "no_user_risk_level"
    }
    if ![no_user_risk_level] {
      mutate {
        merge => {
          "additional.fields" => "userRiskLevellabel"
        }
        on_error => "no_user_risk_level_merge_error"
      }
    }
  }
  if [userUniversalId] not in ["", " "] {
    mutate {
      replace => {
        "userUniversalIdlabel.key" => "userUniversalId"
        "userUniversalIdlabel.value.string_value" => "%{userUniversalId}"
      }
      on_error => "no_user_universal_id"
    }
    if ![no_user_universal_id] {
      mutate {
        merge => {
          "additional.fields" => "userUniversalIdlabel"
        }
        on_error => "no_user_universal_id_merge_error"
      }
    }
  }
  if [userRiskScore] != "" {
    mutate {
      replace => {
        "userRiskScorelabel.key" => "userRiskScore"
        "userRiskScorelabel.value.string_value" => "%{userRiskScore}"
      }
      on_error => "no_user_risk_score"
    }
    if ![no_user_risk_score] {
      mutate {
        merge => {
          "additional.fields" => "userRiskScorelabel"
        }
        on_error => "no_user_risk_score_merge_error"
      }
    }
  }
  if [userIp] != "" {
    mutate {
      merge => {
        "principal.ip" => "userIp"
        "principal.asset.ip" => "userIp"
      }
      on_error => "no_user_ip"
    }
    mutate {
      replace => {
        "has_principal" => "true"
      }
    }
  }
  if [__isAuditIdLcmIdStore] != "" {
    mutate {
      replace => {
        "__isAuditIdLcmIdStorelabel.key" => "__isAuditIdLcmIdStore"
        "__isAuditIdLcmIdStorelabel.value.string_value" => "%{__isAuditIdLcmIdStore}"
      }
      on_error => "no_is_audit_id_lcm_id_store"
    }
    if ![no_is_audit_id_lcm_id_store] {
      mutate {
        merge => {
          "additional.fields" => "__isAuditIdLcmIdStorelabel"
        }
        on_error => "no_is_audit_id_lcm_id_store_merge_error"
      }
    }
  }
  if [principalType] != "" {
    mutate {
      replace => {
        "principalTypeLabel.key" => "principalType"
        "principalTypeLabel.value.string_value" => "%{principalType}"
      }
      on_error => "no_principal_type"
    }
    if ![no_principal_type] {
      mutate {
        merge => {
          "additional.fields" => "principalTypeLabel"
        }
        on_error => "no_principal_type_merge_error"
      }
    }
  }
  if [clientTid] != "" {
    mutate {
      replace => {
        "clientTidlabel.key" => "clientTid"
        "clientTidlabel.value.string_value" => "%{clientTid}"
      }
      on_error => "no_client_tid"
    }
    if ![no_client_tid] {
      mutate {
        merge => {
          "additional.fields" => "clientTidlabel"
        }
        on_error => "no_client_tid_merge_error"
      }
    }
  }
  if [identitySourceId] != "" {
    mutate {
      replace => {
        "identitySourceIdlabel.key" => "identitySourceId"
        "identitySourceIdlabel.value.string_value" => "%{identitySourceId}"
      }
      on_error => "no_identity_source_id"
    }
    if ![no_identity_source_id] {
      mutate {
        merge => {
          "additional.fields" => "identitySourceIdlabel"
        }
        on_error => "no_identity_source_error"
      }
    }
  }
  if [stream] != "" {
    mutate {
      replace => {
        "streamlabel.key" => "stream"
        "streamlabel.value.string_value" => "%{stream}"
      }
      on_error => "no_stream"
    }
    if ![no_stream] {
      mutate {
        merge => {
          "additional.fields" => "streamlabel"
        }
        on_error => "no_stream_merge_error"
      }
    }
  }
  if [logtag] != "" {
    mutate {
      replace => {
        "logtaglabel.key" => "logtag"
        "logtaglabel.value.string_value" => "%{logtag}"
      }
      on_error => "no_logtag"
    }
    if ![no_logtag] {
      mutate {
        merge => {
          "additional.fields" => "logtaglabel"
        }
        on_error => "no_logtag_merge_error"
      }
    }
  }
  if [type] != "" {
    mutate {
      replace => {
        "typelabel.key" => "type"
        "typelabel.value.string_value" => "%{type}"
      }
      on_error => "no_type"
    }
    if ![no_type] {
      mutate {
        merge => {
          "additional.fields" => "typelabel"
        }
        on_error => "no_type_merge_error"
      }
    }
  }
  if [remoteAddr] not in ["", "-"] {
    mutate {
      replace => {
        "remoteAddrlabel.key" => "remoteAddr"
        "remoteAddrlabel.value.string_value" => "%{remoteAddr}"
      }
      on_error => "no_remote_addr"
    }
    if ![no_remote_addr] {
      mutate {
        merge => {
          "additional.fields" => "remoteAddrlabel"
        }
        on_error => "no_remote_addr_merge_error"
      }
    }
  }
  if [remoteUser] not in ["", "-"] {
    mutate {
      replace => {
        "remoteUserlabel.key" => "remoteUser"
        "remoteUserlabel.value.string_value" => "%{remoteUser}"
      }
      on_error => "no_remote_user"
    }
    if ![no_remote_user] {
      mutate {
        merge => {
          "additional.fields" => "remoteUserlabel"
        }
        on_error => "no_remote_user_merge_error"
      }
    }
  }
  if [requestTime] not in ["", "-"] {
    mutate {
      replace => {
        "requestTimelabel.key" => "requestTime"
        "requestTimelabel.value.string_value" => "%{requestTime}"
      }
      on_error => "no_request_time"
    }
    if ![no_request_time] {
      mutate {
        merge => {
          "additional.fields" => "requestTimelabel"
        }
        on_error => "no_request_time_merge_error"
      }
    }
  }
  if [vhost] not in ["", "-"] {
    mutate {
      replace => {
        "vhostlabel.key" => "vhost"
        "vhostlabel.value.string_value" => "%{vhost}"
      }
      on_error => "no_vhost"
    }
    if ![no_vhost] {
      mutate {
        merge => {
          "additional.fields" => "vhostlabel"
        }
        on_error => "no_vhost_merge_error"
      }
    }
  }
  if [contentType] not in ["", "-"] {
    mutate {
      replace => {
        "contentTypelabel.key" => "contentType"
        "contentTypelabel.value.string_value" => "%{contentType}"
      }
      on_error => "no_content_type"
    }
    if ![no_content_type] {
      mutate {
        merge => {
          "additional.fields" => "contentTypelabel"
        }
        on_error => "no_content_type_merge_error"
      }
    }
  }
  if [requestId] != "" {
    mutate {
      replace => {
        "requestIdlabel.key" => "requestId"
        "requestIdlabel.value.string_value" => "%{requestId}"
      }
      on_error => "no_request_id"
    }
    if ![no_request_id] {
      mutate {
        merge => {
          "additional.fields" => "requestIdlabel"
        }
        on_error => "no_request_id_merge_error"
      }
    }
  }
  if [clientTxnId] not in ["", "-", " "] {
    mutate {
      replace => {
        "clientTxnIdlabel.key" => "clientTxnId"
        "clientTxnIdlabel.value.string_value" => "%{clientTxnId}"
      }
      on_error => "no_client_txn_id"
    }
    if ![no_client_txn_id] {
      mutate {
        merge => {
          "additional.fields" => "clientTxnIdlabel"
        }
        on_error => "no_client_txn_id_merge_error"
      }
    }
  }
  if [txnId] not in ["", "-"] {
    mutate {
      replace => {
        "txnIdlabel.key" => "txnId"
        "txnIdlabel.value.string_value" => "%{txnId}"
      }
      on_error => "no_txn_id"
    }
    if ![no_txn_id] {
      mutate {
        merge => {
          "additional.fields" => "txnIdlabel"
        }
        on_error => "no_txn_id_merge_error"
      }
    }
  }
  if [issuerUrl] != "" {
    mutate {
      replace => {
        "target.url" => "%{issuerUrl}"
      }
      on_error => "no_issuer_url"
    }
  }
  if [flowStateId] != "" {
    mutate {
      replace => {
        "flowStateIdlabel.key" => "flowStateId"
        "flowStateIdlabel.value.string_value" => "%{flowStateId}"
      }
      on_error => "no_flow_state_id"
    }
    if ![no_flow_state_id] {
      mutate {
        merge => {
          "additional.fields" => "flowStateIdlabel"
        }
        on_error => "no_flow_state_id_merge_error"
      }
    }
  }
  if [azpName] != "" {
    mutate {
      replace => {
        "azpNamelabel.key" => "azpName"
        "azpNamelabel.value.string_value" => "%{azpName}"
      }
      on_error => "no_azp_name"
    }
    if ![no_azp_name] {
      mutate {
        merge => {
          "additional.fields" => "azpNamelabel"
        }
        on_error => "no_azp_name_merge_error"
      }
    }
  }
  if [accessTokenTid] != "" {
    mutate {
      replace => {
        "accessTokenTidlabel.key" => "accessTokenTid"
        "accessTokenTidlabel.value" => "%{accessTokenTid}"
      }
      on_error => "no_access_token_tid"
    }
    if ![no_access_token_tid] {
      mutate {
        merge => {
          "security_result.detection_fields" => "accessTokenTidlabel"
        }
        on_error => "no_access_token_tid_merge_error"
      }
    }
  }
  if [accessTokenScopes] != "" {
    mutate {
      replace => {
        "accessTokenScopeslabel.key" => "accessTokenScopes"
        "accessTokenScopeslabel.value" => "%{accessTokenScopes}"
      }
      on_error => "no_access_token_scopes"
    }
    if ![no_access_token_scopes] {
      mutate {
        merge => {
          "security_result.detection_fields" => "accessTokenScopeslabel"
        }
        on_error => "no_access_token_scopes_merge_error"
      }
    }
  }
  if [kubernetes][container_hash] != "" {
    mutate {
      replace => {
        "container_hashlabel.key" => "container_hash"
        "container_hashlabel.value" => "%{kubernetes.container_hash}"
      }
      on_error => "no_container_hash"
    }
    if ![no_container_hash] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "container_hashlabel"
        }
        on_error => "no_container_hash_merge_error"
      }
    }
  }
  mutate {
    replace => {
      "sh_chart" => "%{kubernetes.labels.helm.sh_chart}"
    }
    on_error => "no_sh_chart"
  }
  if ![no_sh_chart] {
    mutate {
      replace => {
        "helm_sh_chartlabel.key" => "helm_sh_chart"
        "helm_sh_chartlabel.value" => "%{kubernetes.labels.helm.sh_chart}"
      }
      on_error => "no_helm_sh_chart"
    }
    if ![no_helm_sh_chart] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "helm_sh_chartlabel"
        }
        on_error => "no_helm_sh_chart_merge_error"
      }
    }
  }
  mutate {
    replace => {
      "helmchartname" => "%{kubernetes.labels.helmChartName}"
    }
    on_error => "no_helmchartname"
  }
  if ![no_helmchartname] {
    mutate {
      replace => {
        "helmChartNamelabel.key" => "helmChartName"
        "helmChartNamelabel.value" => "%{kubernetes.labels.helmChartName}"
      }
      on_error => "no_helmChartName"
    }
    if ![no_helmChartName] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "helmChartNamelabel"
        }
        on_error => "no_helmChartName_merge_error"
      }
    }
  }
  mutate {
    replace => {
      "imageTag" => "%{kubernetes.labels.imageTag}"
    }
    on_error => "no_imageTag"
  }
  if ![no_imageTag] {
    mutate {
      replace => {
        "image_taglabel.key" => "imageTag"
        "image_taglabel.value" => "%{kubernetes.labels.imageTag}"
      }
      on_error => "no_image_tag"
    }
    if ![no_image_tag] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "image_taglabel"
        }
        on_error => "no_image_tag_merge_error"
      }
    }
  }
  mutate {
    replace => {
      "pod-template-hash" => "%{kubernetes.labels.pod-template-hash}"
    }
    on_error => "no_pod-template-hash"
  }
  if ![no_pod-template-hash] {
    mutate {
      replace => {
        "pod-template-hashlabel.key" => "pod-template-hash"
        "pod-template-hashlabel.value" => "%{kubernetes.labels.pod-template-hash}"
      }
      on_error => "no_pod-template-hash"
    }
    if ![no_pod-template-hash] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "pod-template-hashlabel"
        }
        on_error => "no_pod-template-hash_merge_error"
      }
    }
  }
  mutate {
    replace => {
      "pod-ip" => "%{kubernetes.annotations.cni.projectcalico.org_podIP}"
    }
    on_error => "no_pod_ip"
  }
  if ![no_pod_ip] {
    mutate {
      replace => {
        "pod-iplabel.key" => "podIP"
        "pod-iplabel.value" => "%{kubernetes.annotations.cni.projectcalico.org_podIP}"
      }
      on_error => "no_pod_ip"
    }
    if ![no_pod_ip] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "pod-iplabel"
        }
        on_error => "no_pod_ip_merge_error"
      }
    }
  }
  mutate {
    replace => {
      "pod-ips" => "%{kubernetes.annotations.cni.projectcalico.org_podIPs}"
    }
    on_error => "no_pod_ips"
  }
  if ![no_pod_ips] {
    mutate {
      replace => {
        "pod-ipslabel.key" => "podIPs"
        "pod-ipslabel.value" => "%{kubernetes.annotations.cni.projectcalico.org_podIPs}"
      }
      on_error => "no_pod_ips"
    }
    if ![no_pod_ips] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "pod-ipslabel"
        }
        on_error => "no_pod_ips_merge_error"
      }
    }
  }
  if [kubernetes][container_name] != "" {
    mutate {
      replace => {
        "container_namelabel.key" => "container_name"
        "container_namelabel.value" => "%{kubernetes.container_name}"
      }
      on_error => "no_container_name"
    }
    if ![no_container_name] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "container_namelabel"
        }
        on_error => "no_container_name_merge_error"
      }
    }
  }
  if [kubernetes][container_image] != "" {
    mutate {
      replace => {
        "container_imagelabel.key" => "container_image"
        "container_imagelabel.value" => "%{kubernetes.container_image}"
      }
      on_error => "no_container_image"
    }
    if ![no_container_image] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "container_imagelabel"
        }
        on_error => "no_container_image_merge_error"
      }
    }
  }
  if [kubernetes][docker_id] != "" {
    mutate {
      replace => {
        "docker_idlabel.key" => "docker_id"
        "docker_idlabel.value" => "%{kubernetes.docker_id}"
      }
      on_error => "no_docker_id"
    }
    if ![no_docker_id] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "docker_idlabel"
        }
        on_error => "no_docker_id_error"
      }
    }
  }
  if [kubernetes][pod_id] != "" {
    mutate {
      replace => {
        "pod_idlabel.key" => "pod_id"
        "pod_idlabel.value" => "%{kubernetes.pod_id}"
      }
      on_error => "no_pod_id"
    }
    if ![no_pod_id] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "pod_idlabel"
        }
        on_error => "no_pod_id_error"
      }
    }
  }
  if [kubernetes][pod_name] != "" {
    mutate {
      replace => {
        "pod_namelabel.key" => "pod_name"
        "pod_namelabel.value" => "%{kubernetes.pod_name}"
      }
      on_error => "no_pod_name"
    }
    if ![no_pod_name] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "pod_namelabel"
        }
        on_error => "no_pod_name_error"
      }
    }
  }
  if [kubernetes][namespace_name] != "" {
    mutate {
      replace => {
        "namespace_namelabel.key" => "namespace_name"
        "namespace_namelabel.value" => "%{kubernetes.namespace_name}"
      }
      on_error => "no_namespace_name"
    }
    if ![no_namespace_name] {
      mutate {
        merge => {
          "target.resource.attribute.labels" => "namespace_namelabel"
        }
        on_error => "no_namespace_name_error"
      }
    }
  }
  if [api] != "" {
    mutate {
      replace => {
        "apilabel.key" => "api"
        "apilabel.value" => "%{api}"
      }
      on_error => "no_api"
    }
    if ![no_api] {
      mutate {
        merge => {
          "security_result.detection_fields" => "apilabel"
        }
        on_error => "no_api_merge_error"
      }
    }
  }
  if [reqLength] != "" {
    mutate {
      replace => {
        "reqLengthlabel.key" => "reqLength"
        "reqLengthlabel.value.string_value" => "%{reqLength}"
      }
      on_error => "no_req_length"
    }
    if ![no_req_length] {
      mutate {
        merge => {
          "additional.fields" => "reqLengthlabel"
        }
        on_error => "no_req_length_merge_error"
      }
    }
  }

  mutate {
    rename => {
      "azserver-idp-response-verify-cert-ext-idpId" => "azserver_idp_response_verify_cert_ext_idpId"
    }
    on_error => "no_azserver_idp_response_verify_cert_ext_idpId"
  }
  if [azserver_idp_response_verify_cert_ext_idpId] != "" {
    mutate {
      replace => {
        "principal.user.attribute.cloud.project.id" => "%{azserver_idp_response_verify_cert_ext_idpId}"
      }
      on_error => "no_azserver_idp_response_verify_cert_ext_idpId"
    }
  }else if [__THIS_RESOURCE_ID] != "" {
    mutate {
      replace => {
        "principal.user.attribute.cloud.project.id" => "%{__THIS_RESOURCE_ID}"
      }
      on_error => "no_resource_id"
    }
  }else {
    mutate {
      rename => {
        "admin-identityprovider-create-ext-resourceGuid" => "admin_identityprovider_create_ext_resourceGuid"
      }
      on_error => "no_azserver_idp_response_verify_cert_ext_issuer"
    }
    if [admin_identityprovider_create_ext_resourceGuid] != "" {
      mutate {
        replace => {
          "principal.user.attribute.cloud.project.id" => "%{admin_identityprovider_create_ext_resourceGuid}"
        }
        on_error => "no_admin_identityprovider_create_ext_resourceGuid"
      }
    }
  }

  if [msg] != "" {
    mutate {
      replace => {
        "security_result.description" => "%{msg}"
      }
      on_error => "no_msg"
    }
  }
  if [msg] =~ "Internal user created or updated" {
    mutate {
      replace => {
        "metadata.event_type" => "USER_CREATION"
      }
    }
  }
  else if [msg] =~ "Authorization Initiated Succesfully|Authentication Initiated Successfully|Authentication Successful" {
    mutate {
      replace => {
        "metadata.event_type" => "USER_LOGIN"
        "auth_type"        => "SSO"
      }
    }
  }
  else if [msg] =~ "Token Generated|token verified" {
    mutate {
      replace => {
        "metadata.event_type" => "USER_RESOURCE_ACCESS"
      }
    }
  }
  else if [has_principal] == "true" and [has_target] == "true" {
    mutate {
      replace => {
        "metadata.event_type" => "NETWORK_CONNECTION"
      }
    }
  }
  else if [has_principal] == "true" {
    mutate {
      replace => {
        "metadata.event_type" => "STATUS_UPDATE"
      }
    }
  }
  else {
    mutate {
      replace => {
        "metadata.event_type" => "GENERIC_EVENT"
      }
    }
  }

  if [auth_type] != "" {
    mutate {
      replace => {
        "event.idm.read_only_udm.extensions.auth.type" => "%{auth_type}"
      }
      on_error => "no_auth_type"
    }
  }
  if [metadata] != "" {
    mutate {
      rename => {
        "metadata" => "event.idm.read_only_udm.metadata"
      }
    }
  }

  if [principal] != "" {
    mutate {
      rename => {
        "principal" => "event.idm.read_only_udm.principal"
      }
    }
  }

  if [target] != "" {
    mutate {
      rename => {
        "target" => "event.idm.read_only_udm.target"
      }
    }
  }
  if [additional] != "" {
    mutate {
      rename => {
        "additional" => "event.idm.read_only_udm.additional"
      }
    }
  }
  if [network] != "" {
    mutate {
      rename => {
        "network" => "event.idm.read_only_udm.network"
      }
    }
  }
  if [intermediary] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.intermediary" => "intermediary"
      }
      on_error => "no_intermediary"
    }
  }
  if [security_result] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.security_result" => "security_result"
      }
    }
  }
  mutate {
    merge => {
      "@output" => "event"
    }
  }
}
